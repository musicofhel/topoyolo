# Pass 44 — B2 batch-008 reservoir-gs slice — 2026-08-25
**Task:** B2 [in_progress] — batch-008 candidates 01–03 (reservoir-gs), ≤3/pass; index-prose dedup check first.
**Since last pass:** pass 43 closed batch-007 (12/12); orchestrator queued batch-008 (11 candidates: reservoir-gs 6, kuramoto 5); corpus at derived 140.

## What landed
- `19aac72` — B2 pass-44: batch-008 reservoir-gs candidates 01–03 ANNOTATED:
  - **2108.05024** Grigoryeva–Hart–Ortega (2021): Takens' embedding theorem as a special case of generalized synchronization in random linear state-space systems — topological-conjugacy correspondence; conditional contraction = stability.
  - **2401.00885** Hart (2023): quantitative stability margin for that correspondence — reservoir's largest conditional Lyapunov exponent must beat the target's most negative exponent; spectral radius as the controlling hyperparameter.
  - **2501.11357** Fadera (2025): pullback-attractor box dimension ≤ input-sequence dimension — effective low-dimensionality explains reconstruction success.
  - Dual-index filed: `by-domain/dynamical_systems.md` (new "B2 batch-008" section) + `by-structure/optimal_transport.md` (matching-side precedent: MIOFlow / 2409.08768 pattern). Queue file marked ANNOTATED per INGESTION §5 canonical format.
  - Counts to derived 143, deep cells 28→29, README + docs/index.html patched in the SAME commit (sanctioned drift-fix protocol).

## Verification
- Index-prose dedup check FIRST (per orchestrator): grep for `2108.05024|2401.00885|2501.11357|Grigoryeva|conditional Lyapunov|pullback attractor` across papers/ excluding queue → zero hits outside batch-008; all three genuinely new.
- Lint: `python3 scripts/check_structure.py --check` → **0 errors, 1 warning** (baseline: 2 author-year-prose crossrefs). Queue-status lint caught my first marker wording ("CONSUMED (...)"); fixed to canonical `ANNOTATED as <id>`.
- Content conservation: N/A this pass (no migration — three new files; nothing moved or deleted). `git diff --stat` on touched files shows additions only.

## Findings / surprises
- **B3 bridge pair recorded (orchestrator-flagged):** candidate-01 (2108.05024) × `annotations/2409.08768.md`. Both re-derive Takens by changing ambient category: conjugacy/topological side (this paper) vs pushforward/measure-theoretic OT side (2409.08768). The pair is documented in the annotation itself ("B3 bridge-pair note" section) and in both index entries — ready material for a B3 atlas slice on dynamical_systems.md.
- All three papers are same-lineage (Grigoryeva–Hart–Ortega school); together they give the atlas a rare *three-paper arc* within one machine cell: existence of the matching (conjugacy) → its stability margin (Lyapunov gap) → its dimensional capacity bound (pullback attractor). Candidate-04–06 may extend it.
- Process note: sed-based multi-header edits on the queue file mangled headers twice; fixed by regenerating from HEAD content via python. Lesson recorded: edit queue files with whole-file python replaces keyed on exact header strings only.
- README line 49 had pre-existing drift (claimed "23 of 36 cells ≥10" while headline said 28) — repaired to derived 29 alongside the sanctioned patch.

## Blockers / for Aaron
- None new. Standing: ML→InfoTheo and OT→TDA alias ratification still pending from pass 27.

## Next
Batch-008 reservoir-gs candidates 04–06 (then kuramoto group 07–11).
