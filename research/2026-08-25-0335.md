# Pass 20 — B2 slice-11 — 2026-08-25
**Task:** B2 [in_progress] — batch-003 candidates 03–05 [filtration-ph group]; promote-on-encounter where index prose already covers a queue candidate.

**Since last pass:** ef98790 (slice-10 report), 34e8bb2 (candidates 01+02 promoted to annotations/, counts→derived 80).

## What landed
- `15cc9ff` — batch-003 candidate-04 promote-on-encounter: full prose block in
  `by-domain/tda.md` migrated verbatim to
  `annotations/10.1007-s00521-024-10787-x.md` (de Jesus Jr. et al. 2025,
  N-BEATS+TDA financial forecasting); crossrefs repointed in
  `by-structure/filtrations.md` + `phase_transitions.md`; tda.md block replaced
  by pointer stub; counts to derived **81** (TDA row now ParamHom 30,
  Stability 30, NullHyp 18). Candidates 03 + 05 triage-REJECTED with one-line
  reasons in queue file.
- Candidate-03 (CHIRPS precipitation dataset): REJECTED — zero machines;
  'filtration' was a link-forge text-filter artifact.
- Candidate-05 (TopP-S, ESPH molecular descriptors): REJECTED — one machine
  (parameterized homology only); PH-as-descriptor application wrapper, same
  class as batch-002's SNGP reject.
- Orchestrator note executed: checked by-domain/ and by-structure/ prose before
  any fresh annotation — and it paid off immediately (candidate-04 was covered).

## Verification
- `python3 scripts/check_structure.py` → `0 errors, 0 warnings` (81 annotations
  parsed; queue hygiene incl. REJECTED-reason rule green).
- Conservation: body of the migrated block diffed byte-identical against
  `git show HEAD:papers/by-domain/tda.md` extraction (difflib empty; 468 words).
  Command in session log; net tda.md delta is exactly the removed body + stub.

## Findings / surprises
- **Dedup-gap confirmed as systematic**: 2 of the first 4 batch-003 candidates
  (01, 04) were already prose-block-covered. The orchestrator's adopted fix
  (check indices before annotating) caught candidate-04 this pass. The real fix
  remains the A3 debt item: extend lint to fail on em-dash prose blocks so the
  gap closes structurally.
- filtration-ph group yield is poor: 1 promote / 2 rejects from candidates
  03–05; candidate-06 still unconsumed. Machine-term filtering helped vs
  batch-002 but dataset/descriptor papers still slip through on "persistent
  homology" keyword hits.
- **Dynamics×Matching still at 5** (matrix cell): none of candidates 03–05 is
  an assignment-structure paper. No genuine dyn-matching flag this pass.

## Blockers / for Aaron
- None new. Remaining batch-002 HELD-by-orchestrator candidates (22) unchanged.

## Next
- Batch-003 candidate-06 [filtration-ph] triage (last of the group), then move
  to transport group (6 candidates).
