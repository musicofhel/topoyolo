# Pass 41 — B2 — 2026-08-25
**Task:** batch-007 candidates 04–06 (qec-mwpm slice, promote-on-encounter check vs RNN-decoder authors), then null-surrogate 07+ if budget allows.

**Since last pass:** pass 40 (4e62384, 16b46a4, c572da2) — batch-007 opened, qec-mwpm 3/12 consumed (01–03 ANNOTATED), counts to derived 131, lint 0 errors. Orchestrator confirmed slice verified + pushed; flagged tail-masked exit-code fix as good practice to keep.

## What landed
- `9224af5` — batch-007 **qec-mwpm group CLOSED 6/6**: candidates 04–06 ANNOTATED.
  - 04 Baireuther, O'Brien, Tarasinski & Beenakker (2017) → `annotations/1705.07855.md`: LSTM decoder beats MWPM via X–Z error correlations matching factorizes away — joint-vs-marginal gain in decoder form; learned implicit null (no noise model). Filed qec + phase_transitions + composite_systems.
  - 05 Hack, Menti, Lazaro & Paler (2026) → `annotations/2603.05381.md`: BP on the *decoding graph* reaches standalone thresholds near MWPM where Tanner-graph BP has none — failure located in carrier-graph choice, not the algorithm; joins ldpc-bp lineage (cond-mat/9908104, 1905.10031). Filed qec + phase_transitions.
  - 06 Fowler, Whiteside & Hollenberg (2012) → `annotations/1202.5602.md`: O(n²) average time per round for verified-exact MWPM at distances 4–512 — average-case vs worst-case separation as the practicality theorem of matching-based fault tolerance; ancestor complexity anchor of this lineage. Filed qec + optimal_transport (matching machine's typical-case cost).
- Queue statuses updated to `ANNOTATED as <id>` per INGESTION.md §5.

## Verification
- Promote-on-encounter check ran BEFORE annotation: repo-wide grep for
  Baireuther / Beenakker / 1705.07855 / 2603.05381 / 1202.5602 / "Standalone
  Belief Propagation" over by-domain, by-structure, atlas, glossary,
  annotations, inbox*, second/third_pass ledgers — zero hits outside queue
  files. No Wave-era prose blocks for the RNN-decoder authors or any of the three ids.
- Content conservation: N/A (pure additions — 3 new annotation files, index
  appends only; no moved/edited prose).
- `python3 scripts/gen_stats.py`: derived corpus **134** (+3), 36 cells,
  min cell 2, deep cells 28. README/docs/matrix regenerated consistent.
- `python3 scripts/check_structure.py`: exit 0 — 0 errors, 1 warning
  (2 author-year-prose crossrefs, pre-existing baseline debt).
- Honest-exit note: first lint run after filing caught `1202.5602.md`
  unreferenced in by-structure (exit 1) — fixed by adding the
  optimal_transport.md entry rather than masking; re-run exited 0.

## Findings / surprises
- The qec-mwpm slice turned out to be a coherent cross-machine story: 04 gives
  joint-vs-marginal as decoder advantage, 05 shows message passing and perfect
  matching interchangeable on one carrier graph (chain-complex × matching),
  06 supplies the average-case complexity anchor. The six papers now span all
  of Matching/JvM/chain-complex/stability/PH readings of QEC decoding.
- Candidate-06 is the thinnest machine-wise (Matching-core + stability only)
  but was annotated for lineage consistency with pass-40's sparse-blossom
  entry — it is the complexity baseline that 2303.15933 explicitly sharpens.
- Deep cells 27→28: QEC×JvM strengthened again by 1705.07855 alongside 2107.13589.

## Blockers / for Aaron
- None new. ML→InfoTheo / OT→TDA alias ratification still outstanding from pass 27.

## Next
- Null-surrogate candidates 07–12 of batch-007 (≤3 next pass). Per
  orchestrator: B3 PARAMETERIZED_HOMOLOGY two-parameter refinement sub-slice
  may alternate once the qec group's momentum is spent — it now has (group closed),
  so pass 42 can choose either; default to 07–09 null-surrogate.
