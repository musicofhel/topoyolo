# Pass 56 — B2 — 2026-08-25
**Task:** batch-011 opened — fep candidates 01+02 annotated AS A PAIR (claim vs refutation) per orchestrator note; candidate-03 consumed as the pass's third paper. ≤3 papers/pass cap respected exactly.
**Since last pass:** e378af5 queue batch-011 landed (9 candidates); f2277e0 pass-55 report; e6f2f9e B3 sub-slice 8 STABILITY tri-incarnation. Corpus at derived 170.

## What landed
- `7ba1ee5` — batch-011 fep 01+02 ANNOTATED AS A PAIR: `annotations/1906.10184.md` (Friston FEP monograph: blanket conditional independence as composite boundary, NESS attractor as stability certificate) + `annotations/2001.06408.md` (Biehl-Pollock-Kanai: counterexample to the free energy lemma, inequivalent blanket definitions); ANTISYNONYMS entry "Markov-blanket formalism ≠ established lemma". Counts → derived 172.
- `35d5186` — batch-011 fep 03 ANNOTATED as `annotations/2008.03238.md` (Ramstead et al. dual-aspect information geometry; flagged as the lineage's post-critique constructive turn). Counts → derived 173. README/docs headline counts patched both commits (same-commit count protocol).

## Verification
- `python3 scripts/gen_stats.py` after each commit: derived 173 papers, 36 cells, min cell 2, 32 deep.
- `python3 scripts/check_structure.py --check`: **0 errors, 1 warning** (pre-existing author-year-prose crossref debt, unchanged baseline).
- Conservation n/a (pure additions, no migration).
- Queue hygiene: candidate statuses flipped in-place only (`ANNOTATED as <id>`), lint queue checks pass (177 candidates scanned).

## Findings / surprises
- **New null-machinery pattern (derivation-targeted null):** 2001.06408 is the first corpus refutation aimed at a formalism's *derivation* rather than an empirical claim — its witness is a counterexample system, not a surrogate dataset. Noted inline in composite_systems.md and in the ANTISYNONYMS entry. This matters for the soc three-way next pass: the Plenz/Touboul-Destexhe/Dehghani disagreement is empirical-methodological (power-law spuriousness), so it will exercise a *different* null mode — likely needing the corpus to represent multi-party structure rather than a pair.
- The fep group has an internal chronology worth preserving at annotation level: claim (2019) → refutation (2020) → post-critique constructive repair (2020, Ramstead). Ramstead re-founds representation on blanket *structure* rather than the disproven lemma — whether it uses an equivalence-class-safe blanket definition is not checkable from the abstract; flagged for full-depth follow-up if a PDF ever arrives.
- Same-commit count protocol required two patches of docs/index.html (README sed hits one string, docs has two separate occurrences — `<span>` form differs). No drift escaped; noted for future passes doing count bumps.

## Blockers / for Aaron
- None new. Standing: GIT≠persistence-stability ANTISYNONYMS hook remains open as a B3 alternate.

## Next
Consume soc candidates 05–07 as the THREE-WAY structured disagreement (Plenz affirmative vs Touboul-Destexhe methodological null vs Dehghani empirical refutation). Before annotating, decide how null machinery represents three parties — options seen this pass: pair-of-pairs (like rg-dl), or a dedicated multi-party note in ANTISYNONYMS/composite_systems mirroring the derivation-null pattern. Then candidate-04 triage (likely REJECTED: application survey, ≤2 machines) and 08–09.
