# Pass 29 — B2 slice-29 — 2026-08-25
**Task:** Close batch-004: candidates 15 (cond-mat/9908358 Vicente–Saad–Kabashima irregular LDPC) + 17 (1109.3041 Decelle et al. SBM phase diagram — new canonical detectability-transition anchor per orchestrator note); then sanctioned wrong-pointer lint task.
**Since last pass:** ca68516 (pass-28 report), e49f204 (slice-28 ldpc-bp 14+16), a711fd6 (slice-28 candidate-13); corpus at derived 104.

## What landed
- `45955a1` — slice-29: **batch-004 CLOSED 17/17.** Candidate-15 annotated as
  `annotations/cond-mat-9908358.md` (replica method places the irregular-LDPC
  decoding transition *on* Shannon's coding bound; degree-distribution
  irregularity as threshold-placement engineering). Candidate-17 annotated as
  `annotations/1109.3041.md` (Decelle et al. SBM cavity phase diagram:
  detectability/undetectability = null-hypothesis boundary, easy/hard =
  statistical-to-computational gap; filed as canonical detectability-transition
  anchor, crosslinked to 1905.10031's strictly-below-KS result). Filed in
  by-domain/{information_theory,statistical_physics}.md +
  by-structure/phase_transitions.md; queue statuses flipped; README + docs/index.html
  counts 104→106.
- `fecaeb7` — sanctioned wrong-pointer sweep: validated every
  `annotations/*.md` reference target exists (106/106 resolve); repaired 9
  high-confidence wrong `Full annotation:` pointers via inline-id resolution.

## Verification
- `python3 scripts/check_structure.py` → 106 annotations parsed, 0 errors,
  1 warning (pre-existing baseline: 2 author-year-prose crossrefs). Exit 0 on
  both commits.
- Conservation: no annotation content moved or rewritten this pass — two new
  files added (104→106), index additions only, pointer-repair commit touches 9
  lines changing only the target filename inside backticks (diff --stat: 9+/9−).

## Findings / surprises
- The wrong-pointer debt has structure: Wave-1–10-era entries wrote
  `Full annotation: annotations/2508.19048.md` (or Tsuda / 0711.0468 /
  tort-2010 / 1703.00810) as a catch-all regardless of the entry's paper.
  ~113 such pointers remain across by-* files (2508.19048 ×~38, 0711.0468
  ×~26, Tsuda ×~19, 1703.00810 ×~16, tort-2010 ×~14).
- Two repair classes: (a) entry carries an inline id in the parenthetical →
  mechanical fix (9 done this pass; correct targets all exist, e.g.
  quant-ph-9906129.md); (b) hint says only "(Wave N)" → needs per-entry
  title→annotation lookup, ~5/pass cadence like prior passes.
- A naive author-year heuristic over-flags (~126) because See-also crossrefs to
  related papers are legitimate; the reliable signal is "Full annotation:"
  lines whose inline id contradicts the target.

## Blockers / for Aaron
- None new. Standing: ML→InfoTheo and OT→TDA alias ratification (pass 27);
  batch-005 foraging queued on orchestrator side.

## Next
- Continue wrong-pointer class-(b) repairs at ≤5/pass (start with
  phase_transitions.md Stability section, where Aharonov–Ben-Or / Knill /
  Breuckmann entries now correctly point but their Wave-6 neighbors still point
  at 2508.19048), until batch-005 lands.
