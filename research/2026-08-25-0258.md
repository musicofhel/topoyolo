# Pass 17 — B2 slice-8 — 2026-08-25
**Task:** B2 batch-002, continue from candidate-13 (atlas-general tranche; orchestrator notes: high-forgeScore papers here, target cells Dynamics×Matching (4) and neuroscience; separatrix-tag skepticism confirmed policy).
**Since last pass:** slice-7 (33a73b8, 890bb15) — candidates 05/06/08/11/12 rejected, 12/40 consumed.

## What landed
- `3b46b92` — batch-002 candidates **13, 14, 15 triage-REJECTED**, batch-002 now
  **15/40 consumed, 25 queued**. Counts unchanged at derived 78 (no annotations).
  - **candidate-13** — Kollár–Miyaoka–Mori 1992 (rational connectedness/boundedness of Fano manifolds): pure algebraic geometry, zero machines.
  - **candidate-14** — Osher–Sethian 1987 level-set/PSC algorithms: numerical front-tracking (Hamilton-Jacobi schemes); only atlas-adjacent content is incidental topological merging/breaking — not a machine instantiation (<2 machines; separatrix is not a machine per A6).
  - **candidate-15** — Barandes 2309.03085 (stochastic-quantum theorem): Dynamics-relevant and the strongest paper of the three, but on strict reading instantiates only ONE machine (joint-vs-marginal excess in the composite-system/subsystem construction). No chain complex, matching, stability bound, or surrogate/null test in the extract.

## Verification
- `python3 scripts/check_structure.py --check` → exit 0, "0 errors, 25 warnings"
  (warnings are the pre-existing gen_stats domain-alias class).
- No annotations this pass → no content-conservation proof required (rejections are
  one-sentence status changes per INGESTION §2/§4; batch file history preserved).

## Findings / surprises
- The orchestrator's optimism about the atlas-general tranche did NOT hold for its
  first three papers: two are classic pure-math/numerics papers with no ML/information
  content at all, and the third (Barandes) is genuinely interesting but single-machine.
- **Preview of candidates 16–18 (read during triage, NOT consumed):** 16 = Ein–Küchle–Lazarsfeld
  Seshadri constants (pure alg-geom, likely reject); 17 = SNGP uncertainty estimation
  (one machine: Stability/Lipschitz via spectral normalization — likely reject unless a
  second machine is defensible); 18 = Dean BSM-as-flat-limit information geometry SSRN
  draft v0.18 (Fisher-manifold finance; Stability/attractor + possible phase-transition
  structure, but single-author draft quality flag). Expect another thin slice.
- Pattern worth flagging for the orchestrator: the "atlas-general" tranche looks like a
  catch-all export (classic math papers + ML wrappers), not a high-forgeScore cut. If
  slices 9–10 stay all-reject, consider re-exporting with a machines-filtered query.

## Blockers / for Aaron
- None hard. Queue request for orchestrator: see finding above — the atlas-general
  tranche's hit rate so far (3 rejects / 3) contradicts the "expect better annotate
  rates" note.

## Next
Slice-9: consume candidates 16–18 from batch-002 (triage previews already recorded
above to speed the next pass up); if any is annotated, dual-index + matrix regen +
lint before commit.
