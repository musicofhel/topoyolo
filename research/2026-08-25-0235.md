# Pass 14 — B2 slice-5 — 2026-08-25
**Task:** B2 consume queue batches — start batch-002 (40 candidates), same triage policy; plus quick fix of the blahut-arimoto wrong-pointer debt in `by-structure/optimal_transport.md`; report whether Dynamics×Matching cleared its threshold.
**Since last pass:** 4670350 (slice-4 report+ledger; batch-001 fully consumed, corpus 77).

## What landed
- `0cb95d1` — fixed **5 wrong-pointer crossrefs** in
  `papers/by-structure/optimal_transport.md`: the Thual (2206.09398), Janati
  (1902.04812), Lee (1906.11768), Bubenik & Elchesen (1912.02563), and Chen &
  Wang (2104.07710) entries all claimed `annotations/quant-ph-0110143.md`
  (Dennis et al. topological memory) as their full annotation; each now points
  at its own arXiv-named file (all five files already exist). The two genuine
  Dennis pointers (lines 48/51 area) were left alone.
- `558c2fe` — batch-002 opened: candidates 01–03 triage-REJECTED with one
  sentence each (see Findings). Queue status lines updated in place; no
  annotation content touched.
- `e90a4a8` — README.md + docs/index.html headline counts patched to derived
  truth (**77 papers, 20 cells ≥10, min cell 2**) — drift left by slice-4's
  four additions, which had not regenerated the claims. `--check` now 0 errors.

## Verification
- `python3 scripts/check_structure.py` → **0 errors, 0 warnings** (after each
  content commit).
- `python3 scripts/check_structure.py --check` → was **2 errors** (README/docs
  count drift) before `e90a4a8`, **0 errors / 25 warnings** after. The 25
  warnings are all gen_stats "unrecognized domain(s)" alias notes — a
  pre-existing debt class (was 18 informational notes at A4; grew to 25 as new
  annotations used free-form domain tags). Not introduced by this pass;
  catalogued, unfixed.
- Conservation: this pass moved no annotation prose — pointer strings and queue
  status headers only (`grep -c '^## .* ---'` corpus unchanged at 77 parsed
  annotations per both lint runs).

## Findings / surprises
- **Dynamics×Matching has NOT cleared its threshold**: gen_stats at pass start
  reports **77 papers**, Dynamics×Matching = **4** vs the deep-cell bar of ≥10.
  It remains one of the two thinnest cells (QEC×JointMarg = 2). slice-4's two
  pure-Matching papers (ying-2016, silva-2018) were filed Matching-only, so they
  didn't move the Dynamics row — future Dynamics×Matching candidates should be
  annotated with both cells where genuinely warranted.
- batch-002 opens with a `[geometric-dl]` run of near-uniform character:
  candidate-01 Tunable Soft Equivariance (vision-engineering, zero machines),
  candidate-02 Geometric Hawkes Processes w/ graph-conv RNNs (application
  wrapper over already-covered Hawkes excitation), candidate-03 Allegro
  (equivariant interatomic potentials). All three rejected under the <2-machines
  rule. The batch's higher-value sections (rate-distortion, channel capacity,
  LDPC, stochastic thermodynamics, separatrix) start at candidate-04+ /
  `[ldpc-coding]` / `[separatrix]` tags.
- Wrong-pointer class is bigger than the catalogue entry suggested: INDEX said
  3 instances; grep found 5 in optimal_transport.md alone. Other by-structure
  files were clean for this pointer target.

## Blockers / for Aaron
- None. (Standing: the ~51 hidden prose annotations in index files still need
  systematic promotion — grep-by-title remains necessary until then.)

## Next
- Pass 15 = B2 slice-6: continue batch-002 from candidate-04 onward, prioritizing
  the rate-distortion/channel-capacity/LDPC/stochastic-thermodynamics sections
  (they feed the Blahut–Arimoto matching foundations directly) and any
  Dynamics×Matching fit; ≤3 candidates, same triage policy.
