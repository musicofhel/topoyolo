# Pass 30 — B2 slice-30 — 2026-08-25
**Task:** B2 [in_progress] — consume batch-005 channel-capacity group (candidates 01–07), ≤3 annotations per pass; sanctioned wrong-pointer repairs ≤5 between slices.
**Since last pass:** dd8aec0 (batch-005 queued), fecaeb7+45955a1 (batch-004 CLOSED, pointer sweep), 2e88aad (pass-29 report).

## What landed
- `43b4c56` — batch-005 channel-capacity: 3 ANNOTATED —
  - candidate-04 **Choi, Bao, Qi & Altman (2019)** → `annotations/1903.05124.md` (measurement-induced entanglement transition AS a QEC transition; feeds the thin QEC×Joint-vs-Marginal cell as the orchestrator intended). Machines: joint-vs-marginal (core), PH, stability, null.
  - candidate-03 **Qian & Roy (2011)** → `annotations/1112.4589.md` (channel capacity zero iff free-energy expenditure zero; cascades as distributed multistage codes). Cleanest capacity↔dissipation bridge in the corpus.
  - candidate-06 **Plenio & Virmani (2007)** → `annotations/quant-ph-0702059.md` (capacity under correlated error ↔ spin-chain criticality; i.i.d.-error channels as null).
  - Dual-index filed in `by-domain/qec.md`, `by-domain/information_theory.md`, `by-domain/statistical_physics.md`, `by-structure/phase_transitions.md`; queue statuses set per INGESTION §5 (`ANNOTATED as <id>`).
- `01049fe` — sanctioned wrong-pointer sweep (3 of ≤5): Trinca 2410.20233 qec entry → `annotations/2410.20233.md`; Hamilton–Leditzky 2307.07492 → its own file; Berry et al. 2209.13581 → its own file (all had pointed at catch-all `annotations/0711.0468.md`; all targets verified to exist first).

## Verification
- `python3 scripts/check_structure.py` → **0 errors, 1 warning** (the pre-existing 2 author-year prose crossrefs reported as one WARN line).
- `python3 scripts/gen_stats.py --check` → exit 0 (README/docs/matrix consistent with derived truth).
- Derived corpus now **109 fully annotated papers**, 36 cells, min cell 2, **26 cells ≥10** (was 23).
- Conservation: this pass created new files and appended index entries — nothing migrated or deleted; `git show --stat 43b4c56` shows only adds/appends.

## Findings / surprises
- The lint's queue-hygiene status vocabulary is strict (`ANNOTATED as <id>` / REJECTED / UNCONSUMED) — my first commit attempt used free text and was caught; format now matches INGESTION §5.
- Annotation headers must be `## <id> --- <authors>` (H2); I initially wrote H1 — lint caught it. Both catches on the first lint run, fixed before commit.
- batch-005 channel-capacity is high-quality foraging: candidates 03/04/06 each instantiate ≥3 machines cleanly. Remaining channel-capacity candidates 01 (Bereyhi all-or-nothing = capacity via replica), 02 (Kabashima TAP/LDPC — likely near-duplicate of the cond-mat/9908358 + 1307.5210 lineage already annotated; triage-reject probable), 05 (Kelly et al. coherence game — second QEC feed, strong), 07 (cardiac wavebreak — likely one-machine application wrapper) go to slice-31.
- Wrong-pointer debt note: the qec.md Wave-7/10c entries used `` `.md` (Wave N) `` with backticks mid-pattern — sed repairs must account for that punctuation shape.

## Blockers / for Aaron
- Still pending ratification from pass 27: ML→InfoTheo and OT→TDA alias calls.

## Next
Slice-31: batch-005 channel-capacity remainder — triage candidates 01/02/05/07 (annotate ≤3; expect 02 and 07 to be rejects), then open ph-stability 08–12. Residual Wave-era catch-all pointers continue at ≤5/pass (qec.md still has five `0711.0468` catch-all pointers at lines 138–175 needing per-entry title→annotation resolution).
