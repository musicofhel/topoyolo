# Pass 2 — A2 — 2026-08-24
**Task:** A2 Cross-ref + link debt paydown — tighten prose-only crossrefs,
normalize header grammar. Mechanical only; no annotation content changes.
**Since last pass:** A1 done (e6d03df + 51fa545); ledger moved to repo-root
LOOP_MISSION.md (ae0214e). Baseline: 0 errors, 2 warnings (9 loose crossrefs;
219-vs-53 count).

## What landed
- `6f10501` loop: A2 tighten 9 prose-only crossrefs to carry annotation ids
  (+ `eLife 03476` → `eLife.03476` normalization in 3 index files)
- `b673c24` loop: A2 normalize header grammar — promoted 6 archive `###`
  paper headers to `##`, added arXiv id to the Rosas et al. (2020) header

## Verification
`python3 scripts/check_structure.py` → **exit 0, 0 errors, 1 warning**
(was 2 warnings / 9 loose refs at baseline):
- crossref-debt warning **eliminated** (9 → 0)
- annotations parsed **53 → 60** (6 archive headers now visible + Rosas)
- remaining warning is the expected `claimed count 219 papers != 60
  annotation headers` — deferred to A3/A4 per orchestrator instruction.

Conservation proof: `git diff --stat` for b673c24 = 7 insertions / 7 deletions,
all single header lines (`### id — authors` → `## id — authors`; one header
gained its arXiv id). No annotation body text touched. For 6f10501 the diff is
7 one-line index edits, each inserting only an `arXiv:` token into an existing
cross-reference line (grep counts of added/removed non-header lines symmetric).

## Findings / surprises
- Several of the 9 "loose" matches were **spurious**, not just loose:
  e.g. Wang (2026) matched by-structure/boundary_operators.md via the surname
  "Wang" (Freedman–Kitaev–Larsen–Wang 2001) co-occurring with a 2026 date
  stamp; Thual matched via "Tran" inside the word "Transitions". Soft
  author-year matching can silently vouch for the WRONG paper. Grokking had no
  genuine by-structure entry — a real dual-index gap; fixed by adding an entry
  under phase_transitions.md ("From Second Pass + Bridges → Stability").
- Header grammar is now uniform enough that every explicit-id annotation
  parses. Two author-only headers remain deliberately un-normalized because
  they have no machine-usable id in the annotation body:
  `inbox.md:846 ## Blahut (1972) + Arimoto (1972)` (two papers, one block —
  needs an A3 policy call: split into two files or pick a composite slug) and
  the archive's Takens (1981) / Sauer et al. (1991) blocks (no arXiv/DOI
  anywhere in the block; they match by name-year only).
- The 219 claim vs 60 parsed: gap narrows each time inline by-domain
  annotations are promoted, but true closure needs A3's canonical per-paper
  layout + A4 derived stats.

## Blockers / for Aaron
- None blocking. For A3: decide the Blahut+Arimoto two-papers-one-block case
  (recommendation here: split into two per-paper files at migration time,
  since they are genuinely distinct IEEE Trans. IT 1972 papers).
- Spurious soft-matching suggests check_coverage's author-year fallback should
  eventually require the year to be adjacent to the surname, or be dropped once
  A3 gives every paper an id-bearing file.

## Next
A3 per-paper annotation files — first slice: migrate inbox-archive.md Wave 1–2
blocks to `papers/annotations/<slug>.md` (verbatim moves), shrink the archive
to queue-contract + wave index, update crossrefs, extend the lint to enforce
the new layout. Lint green at every commit.
