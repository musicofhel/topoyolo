# Pass 5 — A3 slice 3 — 2026-08-24
**Task:** A3 [in_progress] Per-paper annotation files — slice 3: migrate the
Phase 2 section of `papers/inbox-archive.md` to `papers/annotations/`, repoint
the Core ATT stubs at the per-paper files.
**Since last pass:** slices 1–2 landed (1c96d87, 5771173 + reports); 22
per-paper files existed; lint green at 467f7f9.

## What landed
- `4b6db24` — A3 slice 3:
  - 9 new verbatim per-paper files: `quant-ph-0101025` (Freedman et al.
    2001), `2106.04024` (Barannikov et al. 2021), `0711.0468` (Bombin &
    Martin-Delgado 2007), `10.1371-journal.pcbi.1003588` (Oizumi et al.
    2014), `10.1038-s41586-021-04268-7` (Gardner et al. 2022),
    `10.1126-science.1227079` (Sugihara et al. 2012), `1507.06217`
    (Adams et al. 2017), `1703.00810` (Shwartz-Ziv & Tishby 2017),
    `10.1017-S0140525X01000097` (Tsuda 2001). Slug convention unchanged
    (id with `/`→`-`; DOI kept as publisher slug).
  - Archive Phase 2 section replaced with a pointer list, same idiom as
    slices 1–2.
  - 4 Core ATT `###` stubs that said "**Full annotation below**"
    (Adams, Gardner, Sugihara, Tsuda) now link their per-paper files —
    this is the Gardner/Sugihara/Tsuda Core ATT unblock the orchestrator
    flagged. Cohen-Steiner and Giusti stubs already pointed at files.

## Verification
- `python3 scripts/check_structure.py` → **0 errors, 1 warning**, exit 0
  (65 annotations parsed). The warning is the known baseline debt
  (README/docs claim 219 vs parseable headers) — it shrinks as inbox.md
  Waves 4+ migrate in slice 4; exact-count agreement stays deferred to A4.
- Conservation proof: reassembled the 9 new files and `diff`ed against the
  extracted archive block (`/tmp/srcbody.md` vs `/tmp/recon.md`) → only two
  differences, both structural not contentual: the section's own leading
  blank/`---`/blank divider, and a trailing newline on the last file.
  Word census: source block 5,948 words (incl. 6-word section heading +
  divider) vs 5,940 across the nine files. No annotation text touched.
- `inbox-archive.md` now contains **zero** full annotations — only wave
  pointer lists and the six prose Core ATT stubs (which are pointers, not
  annotations).

## Findings / surprises
- Orchestrator's "~12 papers" for Phase 2 was actually **9** — the section
  held exactly nine `## <id> --- <authors>` blocks.
- With slice 3 done, `inbox-archive.md` is fully decomposed. All remaining
  monolith annotations live in `papers/inbox.md` (Waves 4+).
- Lint annotation count reconciles exactly: 31 per-paper headers (22 ASCII
  `---` + 9 em-dash `—` separators, both accepted by HEADER_RE) + 34
  remaining in inbox.md (Waves 4+) = 65 parsed. The migration itself is
  count-neutral by design (headers move, they don't multiply); the earlier
  "60" in A2's baseline predates the slice-1/2 file creation.

## Blockers / for Aaron
None this pass.

## Next
A3 slice 4: migrate `papers/inbox.md` Waves 4+ annotations (grep `^## .* --- `
in inbox.md windowed by header offsets — never whole-read it); then both
inboxes shrink to queue contract + leads + per-file index, and A3 flips done.
