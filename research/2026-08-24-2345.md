# Pass 6 — A5 — 2026-08-24
**Task:** A5 [open] Loose-file adjudication — second_pass.md, third_pass_*.md, cross_domain_bridges.md: fold or document, no deletions. Done = papers/ has a written layout contract and no undocumented files.
**Since last pass:** A4 done (a49e738+db97bca); orchestrator dropped queue batch-001 (27d8938) — reserved for Phase B, NOT consumed this pass.

## What landed
- `61fee02` — **papers/README.md layout contract.** Every entry under papers/ is
  assigned a role: canonical corpus (`annotations/`, `by-domain/`,
  `by-structure/`), intake (`inbox.md`, `inbox-archive.md`, `queue/`), and
  search-pass ledgers (historical, non-canonical: `second_pass.md` SP-01…17,
  `third_pass_dynamics_tda.md` TP 16, `third_pass_infotheo_cross.md` TP 9,
  `third_pass_neuro_qec.md` TP 12, `cross_domain_bridges.md` 13 entries).
  Ledger rule written down: entries leave only by promotion
  (annotate → annotations/<id>.md → dual index → matrix regen), never by
  deletion; ledger prose stays put.
- Same commit — **check_structure.py extended**: every file/dir directly under
  `papers/` must be named in `papers/README.md`, else lint error. The
  "no undocumented files" criterion is now machine-enforced, not a one-time
  audit.
- Adjudication decision: these are candidate ledgers from the April-2026
  link-forge passes, predating the per-paper layout. Folding ~2,300 lines into
  per-paper files + dual index is per-paper METHODOLOGY-depth work (= Phase B
  shape), not a mechanical move — so option (b) document, not (a) fold, for all
  five files. Promotion happens through the normal ingestion path, batch-sized.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors**, 18 warnings
  (all pre-existing A4 gen_stats domain-alias informational notes; unchanged
  baseline). New line: `papers/ entries : 11 documented in layout contract`.
- Content conservation: nothing moved — this pass only ADDED files
  (papers/README.md) and lint logic. All five loose files byte-identical to
  pre-pass (`git show --stat 61fee02` touches only README/lint/report).
  Ledger-entry counts recorded above for future promotion accounting:
  17 + 16 + 9 + 12 + 13 = 67 candidate entries across the ledgers.

## Findings / surprises
- **Index→annotation gap (new debt class):** the lint checks annotations→both-
  indices, but not the reverse. Two index prose crossrefs point at papers with
  NO per-paper annotation file: Mollers et al. 2023 (Hodge-aware contrastive
  learning, `by-structure/filtrations.md`) and Mézard & Mora (`by-domain/qec.md`
  + infotheo/neuroscience). Both are ledger candidates awaiting promotion.
  Worth a small A-side task or first Phase-B picks.
- The ledgers' summary tables (e.g. third_pass_infotheo_cross.md's
  zero-result-search gap list: rate-distortion, channel capacity, geometric DL,
  LDPC/algebraic coding, stochastic thermodynamics) read like a ready-made
  Phase-B target list — flagged for the orchestrator.
- Candidate-count coincidence: 67 ledger candidates ≈ current 68-paper corpus;
  if even half promote cleanly, the corpus roughly doubles. Stats claims will
  need regen discipline each time (A4 tool already enforces).

## Blockers / for Aaron
- None hard. Decision taken without you (documented above): all five loose
  files documented rather than folded — reversible if you'd rather they be
  promoted outright.
- Orchestrator: consider seeding a future queue batch from the ledgers'
  strongest un-promoted candidates (bridging ≥3 domains) before fresh sweeps.

## Next
- A5 → done pending your review of the contract wording. Next task: **A6**
  SEPARATRIX proposal review (read-only; check if `origin/atlas/separatrix`
  is fetchable locally, else record blocker for queue attach). Then B1
  ingestion contract before consuming batch-001.
