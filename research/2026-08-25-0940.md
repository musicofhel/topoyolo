# Pass 58 — B2 batch-011 close — 2026-08-25
**Task:** Close batch-011 (soc 08 = 1807.07213, soc 09 = 1209.3271, fep 04 = 2207.06415 triage) + fix filing debt (2001.06408 missing from by-domain/statistical_physics.md).
**Since last pass:** pass 57 landed soc 05–07 three-way structured disagreement (2102.09124 / 0910.0805 / 1203.0738), ANTISYNONYMS role-map entry; corpus derived 176. Orchestrator confirms verified+pushed; structured-disagreement idiom provisionally ratified by Aaron.

## What landed
- `a7e1c4c` — batch-011 CLOSED: soc 08 ANNOTATED as 1807.07213 Moosavi, Montakhab & Valizadeh (oscillation-criticality coexistence in an oscillatorily-driven Zhang model; optimal amplification AT the critical point → criticality-as-resonance; filed neuroscience + statistical_physics + phase_transitions). soc 09 ANNOTATED as 1209.3271 Girardi-Schappo, Kinouchi & Tragtenberg (synaptic noise as sufficient avalanche generator — noise-induced rather than self-organized; explicit subsampling analysis = simulation cousin of the measurement-channel critique; same filing). fep 04 = 2207.06415 REJECTED on triage (DL implementation survey / practitioner guide, no abstract-machine instantiation) with the lint-required one-sentence reason under the header. Filing debt FIXED: 2001.06408 backfilled into by-domain/statistical_physics.md with pair note. Counts regen to derived 178; docs/index.html (both occurrences) + README.md patched same-commit per standing policy.
- Ledger B2 updated to closed/idle; INDEX rewritten.

## Verification
- `python3 scripts/check_structure.py` → exit 0; **0 errors, 1 warning** (the pre-existing baseline crossref-prose debt).
- `python3 scripts/check_structure.py --check` → 0 errors.
- `python3 scripts/gen_stats.py` → 178 fully annotated papers, 36 cells, min cell 2, 32 cells ≥10; wrote diagrams/coverage-matrix.md (in commit a7e1c4c).
- Content conservation: this pass ADDED content only — two new annotation files, index entries appended in existing idiom, no annotation text moved or deleted. The only rewritten line is the phase_transitions.md trailing italic pointer (a navigation note, not annotation content); grep of `^## .* ---` totals unchanged for all pre-existing files.

## Findings / surprises
- The soc group closes at five entries that now form a complete dialectical structure around the ANTISYNONYMS role map: claim (2102.09124) / methodological null (0910.0805) / empirical null (1203.0738) / mechanism-reconciliation (1807.07213, criticality-as-resonance positive control) / noise-mechanism+subsampling artifact (1209.3271). 1807.07213 does not pick a side — it shows coexistence inside a known-SOC model, which is exactly what both camps lack.
- Docs count was stale at 173 (pass 57's 176 never reached docs/index.html/README despite the standing policy) — caught by `--check`; patched to 178 here. Worth watching: the policy exists because this drift recurs.
- Lint taught a convention: REJECTED candidates need the one-sentence reason DIRECTLY under the header, not after the abstract block (INGESTION.md §4 enforced by check_structure.py).

## Blockers / for Aaron
- Structured-disagreement idiom: ratified provisionally per orchestrator; nothing further needed unless Aaron wants the two new "side" labels (mechanism-reconciliation, noise-mechanism+subsampling) reflected in the ANTISYNONYMS role map — currently the map lists claim/methodological-null/empirical-null only, and I left it untouched (extending it felt like a semantics change to ratify, not a filing chore).

## Next
- Batch-011 done; B2 idle until batch-012 lands in papers/queue/. Next pass: B3 sub-slice 9 — draft the "GIT semistability ≠ persistence stability" ANTISYNONYMS entry from 2605.11178 × the persistence-stability column, or idle if Aaron prefers to hold.
