# Pass 21 — B2 slice-12–14 — 2026-08-25
**Task:** B2 [in_progress] — continue batch-003 from candidate-06: filtration-ph remainder, then transport-matching group. Orchestrator: index-prose check first, promote-on-encounter, reject wrappers freely.
**Since last pass:** cff924c (slice-11 report), 15cc9ff (candidate-04 promoted, 03+05 rejected; derived 81).

## What landed
- `c591976` — slice-12: candidates 06 (PHOM), 07 (PHG-Net), 08 (ATPGCN) REJECTED.
- `ce67736` — slice-13: candidates 09 (QR-DQN), 10 (Hawkes FCLT), 11 (GWGAN) REJECTED.
- `1e89b6a` — slice-14: candidates 12 (CDRL), 13 (Cornulier commability), 14 (CP-DRL) REJECTED. Transport-matching group closed 0/6 promoted; batch-003 now 14/28 consumed.

All six were triage-rejections under INGESTION.md §4 (one recorded sentence each in
the queue file); no annotation content touched, counts unchanged at derived **81**.

Rejection classes observed this pass:
1. **PH-as-descriptor / PH-as-feature** (06/07/08): persistent homology used as a
   scalar diagnostic or a feature block inside a CNN/GCN pipeline. Same class as
   batch-002's TopP-S and geometric-dl wrapper rejects.
2. **Transport-as-tool** (09/10/12/14): Wasserstein/Cramér/OT appearing as a loss,
   contraction metric, or convergence-rate bound in RL/probability theory.
3. **Single-machine GW wrapper** (11): GWGAN uses an existing distance as a GAN
   loss; the substantive GW-matching coverage already lives in
   `annotations/2206.09398.md` (FUGW) and `annotations/s41468-022-00089-3.md`
   (merge trees). Candidate 13 is pure geometric group theory — text-filter artifact.

## Verification
- Index-prose check first (per orchestrator): grep for Watanabe/Yamana/PHOM,
  PHG-Net/WACV across papers/, atlas/, glossary/ — no existing coverage for any of
  the six; all six are fresh triage decisions, no promote-on-encounter needed.
- `python3 scripts/check_structure.py --check` → **0 errors**, exit 0 before every
  commit. One process slip caught by the gate itself (see Findings).
- Conservation: trivially satisfied — no migration, queue-file header lines only;
  `grep -c '^## ' papers/queue/batch-003.md` unchanged at 29 (28 candidates + none added).

## Findings / surprises
- The lint gate caught my first commit attempt: I put rejection reasons *inside* the
  header line; check_queue_hygiene requires a one-sentence reason line *under* the
  header. Fixed to match candidate-03's format and amended. Gate works as designed.
- Warning count drifted 18 → 27 → 28 across recent passes: gen_stats emits
  "unrecognized domain" warnings for newer annotation files whose free-text domains
  aren't in the alias table. Pre-existing debt class (catalogued in INDEX), but it
  grows with every promoted paper — a small follow-up task would be extending the
  alias table in scripts/gen_stats.py per A4.
- Group-quality signal for the orchestrator: filtration-ph (6) went 1 promote /
  5 rejects; transport-matching (6) went 0/6. The machine-term filter is pulling in
  heavy RL-theory tail. info-machines (15–20) is next; if it shows the same ratio,
  consider re-weighting the filter toward multi-machine co-occurrence.

## Blockers / for Aaron
None this pass.

## Next
Consume batch-003 info-machines group from candidate-15 (index-prose check first;
promote-on-encounter; reject single-machine wrappers freely).
