# Pass 16 — B2 slice-7 — 2026-08-25
**Task:** B2 [in_progress] — consume queue batches; orchestrator directed: continue
batch-002 from candidate-08; check candidate-11 vs Jost & Zhang 2302.01069;
judge candidate-08's separatrix tag from the extract.
**Since last pass:** slice-6 (a09fe1e+85d8d01): candidate-10 TPCC annotated,
04/07/09 rejected; batch-002 at 7/40.

## What landed
- **890bb15** — batch-002 candidates 05, 06, 08, 11, 12 triage-REJECTED
  (one-sentence verdicts per triage rule §4, recorded in the queue file).
  Batch-002 now **12/40 consumed, 28 queued**. No new annotations this pass →
  headline count stays at derived 78.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors, 25 warnings**
  (all 25 = pre-existing gen_stats domain-alias debt; none introduced).
- Queue hygiene: 5 headers flipped UNCONSUMED→REJECTED with a verdict line
  each; rejections logged in-file, nothing dropped.
- Content conservation: no annotation content touched (triage-only pass);
  corpus file set unchanged (`ls papers/annotations/` same as slice-6).

## Findings / surprises
- **Candidate-08's separatrix tag is confirmed mis-assigned.** CLDNet's
  "decision boundary" is ML classification-boundary language; the SEPARATRIX
  machine needs loss-landscape basins or phase-boundary objects — neither in
  the extract. Flag for the orchestrator: the `separatrix` sweep source is
  matching on the *phrase* "decision boundary", so future separatrix-tagged
  candidates deserve skeptical triage.
- **Candidate-11 vs Jost & Zhang 2302.01069:** genuine technical advance
  (random-walk normalization of coboundary operators, upper-spectrum Cheeger
  bounds) but same machine pair (chain complex + stability), same domain,
  no new bridge → rejected under duplicate coverage. If the atlas ever grows
  a "normalized Hodge theory" thread, revisit.
- **All-rejection pass:** five consumed, zero annotated. The batch-002 tail
  (candidates 13–40, mostly `[atlas-general]`) is where live annotation
  targets must come from — including any Dynamics×Matching candidates for
  the thinnest cell (=4).

## Blockers / for Aaron
- None hard. Request to orchestrator: when exporting batch-003, prioritize
  candidates plausibly hitting **Dynamics × Matching** and neuroscience cells;
  title-grep annotations/ first (adopted duplication rule) should also catch
  the Jost-Zhang/Cheeger class before it reaches the queue.

## Next
- Slice-8: continue batch-002 from candidate-13 (`[atlas-general]` tranche,
  Kollár–Miyaoka–Mori Fano manifolds first). Triage ≤3; annotate anything
  instantiating ≥2 machines, with priority for Dynamics×Matching hits.
