# Pass 37 — B2 slice-37 + B3 sub-slice 3 — 2026-08-25
**Task:** B2 batch-006 close (candidates 07–08; 07 triage candidate) + B3 sub-slice 3 (OT-as-proof-technology inbound to statphys; ldpc ancestor lineage; PH↔Matching bridge hook from candidate-04).
**Since last pass:** slice-36 verified+pushed by orchestrator (8ddf22d); corpus at derived 124; batch-006 at 6/8.

## What landed
- `60fd0d4` — **B2 slice-37: batch-006 CLOSED 8/8.** Candidate-07 Beier–Beinert–Steidl linear GW (2112.11964) REJECTED per triage rule (barycentric-projection linearization = computational accelerator, <2 machines, duplicate GW-theory coverage in 2212.14123/2201.09385). Candidate-08 Wollstadt et al ANNOTATED as `annotations/2203.10810.md` — local info dynamics (AIS/TE) recast predictive-coding strategies as testable constraints, breaking analytical circularity; PID atoms separate bottom-up vs state-conditional transfer on cat retinogeniculate spiking. Filed `by-domain/neuroscience.md` + `by-structure/composite_systems.md`; counts to derived **125**.
- `f758d87` — **B3 sub-slice 3.** New section "Two Cross-Machine Roles" in `atlas/MATCHING.md`: (a) OT-as-proof-technology inbound to statphys (1905.10031: Wasserstein message bounds force the bounded-memory transition below KS — matching-as-lemma); (b) PH→Matching bridge from Chambers–Meng 2507.01171 (extended-persistence summary → persistence image → the mm-space *measure* for Reeb-GW; filtration supplies Matching's ground data). Plus ldpc-bp ancestor lineage note on cond-mat/9908104 in `by-domain/statistical_physics.md`.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors**, 1 warning (2 author-year-prose crossrefs = pre-existing baseline debt).
- Conservation: one new annotation file created verbatim from queue content (no rewrites); queue edits are status-line flips + the §4-mandated one-sentence reject reason under candidate-07's header (lint-enforced); atlas/index touches are additive prose, no existing annotation text moved or deleted.
- gen_stats regenerated matrix at derived truth 125; README/docs count drift (124→125) patched and enforced by `--check`.

## Findings / surprises
- The lint's INGESTION §4 check requires the rejection sentence directly under the header — my first draft put it after the Description block and the lint caught it. Working as designed.
- docs/index.html carries the corpus count in TWO places (og:description meta + hero `<span>`); only the first was caught by sed round 1. Future count bumps should patch both.
- Candidate-08 is a rare instance of joint-vs-marginal machinery used as an *assumption-minimizing methodology* rather than a feature/model component — worth remembering when the neuroscience cells get their next synthesis pass.

## Blockers / for Aaron
- None new. Standing: ML→InfoTheo and OT→TDA alias ratification still pending (pass 27).

## Next
- Fresh session: pick up orchestrator's next directive. Natural candidates: open batch-007 if a queue file lands; otherwise the sanctioned wrong-pointer sweep (~101 residual catch-all pointers, ≤5/pass) or B3 sub-slice 4 (remaining hooks: Monge-vs-Kantorovich internal split from Mémoli–Needham 2212.14123 → optimal_transport.md).
