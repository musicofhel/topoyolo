# Pass 4 — A3 slice 2 — 2026-08-24
**Task:** A3 [in_progress] — per-paper annotation files. Slice 2: migrate inbox-archive
Wave 4b + Wave 3 triage sections to `papers/annotations/`, same verbatim-move
mechanics as slice 1.
**Since last pass:** slice 1 landed (1c96d87 + report 89e1896): Waves 1–2 (15
papers) migrated, crossrefs repointed, lint extended + green.

## What landed
- **5771173** — A3 slice 2: 7 more per-paper files in `papers/annotations/`
  (22 total now):
  - Wave 4b (4): `quant-ph-9707021.md` (Kitaev 1997), `quant-ph-0110143.md`
    (Dennis et al. 2002), `1307.6188.md` (Perea & Harer 2013),
    `math-0604068.md` (Cohen-Steiner/Edelsbrunner/Harer 2007).
  - Wave 3 triage (3): `baudot-2015.md` (### header promoted to ## and given
    the slug id `baudot-2015`; MDPI Entropy has no arXiv id), `2107.09581.md`
    (Bradley 2021), `2405.07665.md` (Kolchinsky 2024).
  - Both archive sections replaced with pointer stubs in slice-1 idiom.
  - Core ATT summary for CSE (2007) repointed from "Full annotation below" to
    its new file.
  - 16 index crossrefs repointed (`Full annotation: \`inbox.md\`` →
    `annotations/<slug>.md`) across by-domain (qec, tda,
    dynamical_systems, information_theory) and by-structure
    (boundary_operators ×3 + Baudot entry, phase_transitions ×2,
    optimal_transport ×2, composite_systems, filtrations).

## Verification
- `python3 scripts/check_structure.py` → exit 0, **0 errors, 1 warning**
  (the pre-existing "claimed 219 vs parsed" count warning — resolves at A4;
  was 65 headers before this pass, still 65 because archive headers became
  file headers one-for-one). No author-year-loose-match warning: all 7 new
  hard keys matched by id, including `baudot-2015`.
- **Conservation proof** (vs `git show HEAD:papers/inbox-archive.md`, lines
  50–227 = old Wave 4b + Wave 3 sections): normalized body lines
  (headers/blank/`---` stripped, whitespace-collapsed) — **117 lines old ==
  117 lines new, Counter diff empty both directions**, i.e. byte-equivalent
  content modulo the 7 rewritten header lines. wc -w: old blocks 1620 vs new
  files 1606; delta = exactly the dropped `##`/`###` header words.

## Findings / surprises
- The "Wave 3 triage annotations" section held a **###-level** Baudot &
  Bennequin block among two ## blocks — promoted during migration, consistent
  with A2's grammar normalization.
- Baudot & Bennequin (2015) is the first annotation with no arXiv id; slug
  fallback `baudot-2015` now serves as its machine key everywhere (both index
  sides carry it). Watch for a second Baudot (2019 TIDA paper already in
  inbox.md Wave 10a) — slugs must stay distinct; they are (`baudot-2015` vs
  future `baudot-2019`/`1907.04242`).
- Remaining archive pointer debt: Core ATT summaries for Gardner/Sugihara/
  Tsuda still say "Full annotation below" — correct today (their full
  annotations are in the Phase 2 section further down the same file); they
  get repointed in slice 3.

## Blockers / for Aaron
- None. Slice 1 verified+pushed by orchestrator; this commit awaits same.

## Next
A3 slice 3: migrate the "Phase 2 Annotations" section of inbox-archive
(12 papers, ~lines 66–384 post-slice-2), including the Gardner/Sugihara/Tsuda
Core ATT pointer updates. After that only inbox.md Waves 4+ remain.
