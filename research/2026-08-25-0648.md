# Pass 28 — B2 slice-28 — 2026-08-25
**Task:** Finish batch-004 (orchestrator-directed): candidate-13 closes rate-distortion, then ldpc-bp 14–17.
**Since last pass:** 695e5ce / c68e06c (slice-27: rate-distortion 10+11 annotated, 12 rejected; counts at derived 101; grid/alias follow-up landed).

## What landed
- `a711fd6` — candidate-13 ANNOTATED as `annotations/2601.16461.md` (Yadav, Song, Shkel & Özgür 2026, log-likelihood distortion: reconstruction scored by −log P_{X|U} against a prescribed generative channel — soft-matching with cross-entropy cost on the coupling; log-loss RD / arbitrary-distortion RD / perfect-perception RD unified in one functional). **Rate-distortion group CLOSED 4/4.** Counts → derived 102.
- `e49f204` — ldpc-bp opened: candidate-14 ANNOTATED as `annotations/1307.5210.md` (Aref–Macris–Vuffray: BP-guided decimation on spatially coupled LDGM; cavity-method phase diagram — dynamical threshold saturates to condensation, condensation approaches the Shannon noise level; Shannon limit = ideal-matcher null) + candidate-16 ANNOTATED as `annotations/1905.10031.md` (Jain–Koehler–Liu–Mossel: bounded-memory message passing transitions strictly below the Kesten–Stigum threshold, EKP conjecture proved; **optimal transport imported as proof technology** for a phase-transition result — a rare OT-inbound bridge). Counts → derived 104.
- Index entries added: `by-domain/information_theory.md`, `by-domain/statistical_physics.md`, `by-structure/phase_transitions.md`, `by-structure/optimal_transport.md`; queue statuses flipped; README/docs/matrix repointed (102→104).

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors**, 1 warning (pre-existing baseline: 2 author-year-prose crossrefs).
- `python3 scripts/gen_stats.py` → derived **104** papers, 36 cells (6×6), min cell 2, 23 cells ≥10; matrix regenerated; README/docs counts bumped and re-checked green before each commit.

## Findings / surprises
- **Ingestion cap binds**: `papers/INGESTION.md` allows ≤3 papers/pass, so "finish batch-004" spans two passes. This pass consumed exactly 3; **candidates 15 (Vicente–Saad–Kabashima replica LDPC) + 17 (Decelle et al. SBM cavity) remain UNCONSUMED.**
- Candidate-16 is the atlas's first clean instance of optimal transport used *inside* a statphys phase-transition proof (OT→statphys direction of import; the corpus is otherwise rich in the reverse direction). Flagged as a B3 synthesis hook.
- Both ldpc-bp papers file under statistical_physics + information theory simultaneously — the batch's stated target (LDPC/BP × phase transitions) is delivering genuine two-domain cells rather than wrappers.
- Decelle et al. (candidate-17) has no existing coverage anywhere in the corpus (`grep -r "Decelle\|1109.3041\|stochastic block"` over annotations: zero hits) — it will be a new canonical detectability-transition anchor when consumed.

## Blockers / for Aaron
- None new (ML→InfoTheo / OT→TDA alias ratification still pending from pass 27).

## Next
Slice-29: consume batch-004 candidates 15 and 17 → **batch-004 CLOSED (17/17)** → begin the sanctioned Full-annotation-target lint check (wrong-pointer class).
