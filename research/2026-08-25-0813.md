# Pass 43 — B2 batch-007 close — 2026-08-25
**Task:** B2 [in_progress] — null-surrogate candidates 10–12 to close batch-007.
**Since last pass:** orchestrator pushed pass-42 work (ce166c7, 75893f9) and queued batch-008
(1b65912: reservoir-gs 6 + kuramoto-transition-theory 5). Corpus at derived 137.

## What landed
- **53f78f4** — batch-007 CLOSED (12/12). Three ANNOTATED, all abstract-only provenance (depth-limited):
  - **1101.6063** Guarin/Delgado/Orozco 2011 → Null + Stability. Band-split surrogate null:
    randomize only high-band Fourier phases so the constructed null retains the
    non-stationarity carrier; rejection licenses "nonlinear beyond what low-band linear drift
    explains" — a design-level narrowing of the Timmer-2000 falsification gap.
  - **1211.1162** Donges/Donner/Kurths 2012 → Null + Joint-vs-marginal. Surrogate-FREE branch of
    the null lineage: time-reversal null tested by direct two-sample comparison of time-directed
    visibility-graph statistics; marginals are reversal-symmetric and blind to it. Joins the
    existing Third-Pass "Visibility Graph Irreversibility" entry (10.3390/e27040402) as its origin.
  - **1306.4064** Small/Judd/Stemler 2013 → Null + Stability. Surrogate machine transplanted to
    complex networks (typical-vs-peculiar under degree-distribution nulls). Rare explicit
    authorial cross-domain bridge: the abstract itself states the analogy to time-series surrogates.
- Same commit: counts to derived **140**, README.md + docs/index.html patched from stale 137
  in the SAME pass as gen_stats ran (sanctioned drift-class fix, see Findings).

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors, 1 warning**
  (the 2 author-year-prose crossref warnings are pre-existing baseline debt, unchanged).
- Content conservation: no migration this pass — all three entries are NEW annotations
  (`wc -w papers/annotations/{1101.6063,1211.1162,1306.4064}.md` = new content only);
  index additions are fresh reference lines, nothing moved or deleted
  (`git show --stat 53f78f4`: only insertions in the two index files).
- `scripts/gen_stats.py --check`: matrix up to date; count enforcement green after patch.

## Findings / surprises
- The sanctioned README/docs-directly protocol was exercised this pass: drift found (stale 137)
  and patched within the same commit as the regen, so lint never sat red between passes.
  Recommend making this standing policy for every B2 slice.
- candidate-11 connects backward to an existing Third-Pass prose entry (e27040402) — the
  visibility-graph irreversibility lineage now has its origin paper; a B3 touch-up could note
  the surrogate-free branch inside phase_transitions.md's null-surrogate block.
- candidate-12 is one of very few papers whose authors THEMSELVES state the cross-domain
  analogy — worth citing as evidence of the atlas's thesis in any write-up.
- **B3 flag (orchestrator request):** batch-008 reservoir-gs candidate-01 (Takens embedding IS
  generalized synchronization) pairs with **2409.08768** (measure-theoretic Takens,
  dyn-matching group, annotated pass 24). When B3 resumes, treat as a bridge pair:
  embedding-as-pushforward ↔ embedding-as-synchronization.

## Blockers / for Aaron
- none

## Next
Batch-008 opens: reservoir-gs group candidates 01–03 (≤3/pass cap), starting with the
Takens/generalized-synchronization bridge candidate.
