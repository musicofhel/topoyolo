# Pass 25 — B2 slice-24 — 2026-08-25
**Task:** B2 queue consumption, batch-004: candidate-04 MIOFlow closes the
dyn-matching group; stoch-thermo group (05–09) next.
**Since last pass:** daf6ffe (slice-23 report+ledger), 4bed853 (slice-23:
candidates 01+02 annotated, 03 TrajectoryNet rejected).

## What landed
- `9028ea5` — candidate-04 MIOFlow ANNOTATED as `annotations/2206.14928.md`
  (dynamic OT + manifold geodesic ground cost; geodesic autoencoder couples
  learned latent metric to the OT cost — neither machine encodes that coupling;
  matching + stability weak). Filed in dynamical_systems + optimal_transport.
  Dyn-matching group closed 4/4.
- `8ca6771` — candidate-05 Nakazato–Ito ANNOTATED as
  `annotations/2103.00503.md` (entropy production ≥ L2-Wasserstein path length;
  thermodynamic speed limits). **New domain index
  `papers/by-domain/statistical_physics.md` opened** for the stoch-thermo
  group. Filed in statistical_physics + optimal_transport.
- `c9ed2cb` — candidate-07 Ito–Oizumi–Amari ANNOTATED as
  `annotations/1810.09545.md` (additivity violation of partial entropy
  productions = stochastic interaction/IIT → joint-vs-marginal excess measured
  in *dissipation units*; Neuroscience↔StatPhys bridge). Filed in
  statistical_physics + composite_systems.
- (final) — crossref fix commit: 1810.09545 was initially missing its
  by-structure ref (lint warn went 2→3); fixed, debt back to baseline.
  Ledger + INDEX updated in the same closing commit.

## Verification
- `python3 scripts/check_structure.py` after every increment: **0 errors,
  1 warning** — the warning is the pre-existing baseline ("2 annotation<->index
  references matched only by author-year prose"); it transiently hit 3 when I
  forgot 1810.09545's by-structure filing and returned to 2 once filed.
- Conservation n/a (no migrations — additive only).
- `python3 scripts/gen_stats.py`: **95 fully annotated papers** (92→95),
  30 cells, min cell 2, 21 deep cells unchanged this pass.
- Queue markers: candidates 04/05/07 marked ANNOTATED in
  `papers/queue/batch-004.md`; batch now 6/17 consumed.

## Findings / surprises
- **Statistical physics is now a corpus domain.** The stoch-thermo group is
  substantial enough (3 more candidates pending: 06, 08, 09) to warrant its own
  by-domain file; gen_stats still reports "6 machines × 5 domains" cell grid,
  so the new domain's cells aren't linted yet — worth a follow-up when the
  domain has ≥3 papers (it already does).
- **Semantic divergence candidate (ANTISYNONYMS):** in TDA/QEC/IT instantiations
  the matching cost measures inference/correction effort; Nakazato–Ito's bound
  makes the same functional measure physical irreversibility (heat/fuel).
  Same math, opposite epistemic direction — flagged in the annotation; a proper
  ANTONYMS entry should wait until candidates 06/09 land so the divergence is
  stated over the cluster, not one paper.
- Candidate-07 is the strongest joint-vs-marginal instantiation found in any
  batch so far: additivity violation with units. It also bridges to the
  existing IIT/synergy rows in neuroscience.md.
- MIOFlow vs TrajectoryNet confirms the reject heuristic: both are
  neural-ODE+dynamic-OT wrappers, but MIOFlow earns annotation via the
  geodesic-autoencoder move (cost metric and representation learned jointly),
  which TrajectoryNet lacks entirely.

## Blockers / for Aaron
- Confirm `statistical_physics` as a standing domain (affects gen_stats cell
  grid and future filing). Default assumption going forward: yes.
- The docs/index.html min-cell/deep-cell lint extension (sanctioned small task)
  was not started this pass — slice budget went to three annotations instead.
  Still available next time a slice runs short.

## Next
Stoch-thermo group continues: candidate-06 (Ito 2022, info geometry × OT
unification — likely annotate), then 08 (Barato–Seifert information reservoirs)
and 09 (Sekizawa et al., oscillatory-mode EP decomposition → neural dynamics —
second Neuroscience bridge). Then rate-distortion 10–13, ldpc-bp 14–17.
