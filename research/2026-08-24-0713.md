# Pass 3 — A3 (slice 1) — 2026-08-24
**Task:** A3 per-paper annotation files — first slice only: migrate inbox-archive Waves 1–2 to `papers/annotations/`, verbatim moves, conservation proof, crossref updates, lint extension.
**Since last pass:** A2 done (6f10501+b673c24, report 0701); orchestrator confirmed A2 verified + pushed.

## What landed
- `1c96d87` — loop: A3 slice 1 — migrate inbox-archive Waves 1-2 to papers/annotations/ (15 verbatim per-paper files), repoint index crossrefs, lint enforces per-paper layout.

Details:
- Created `papers/annotations/` with 15 files, one per paper, content moved
  verbatim: 2604.02033, takens-1981, sauer-1991, 2108.03831, 2508.19048,
  schreiber-2000, tort-2010, 1502.06172, 10.3389-fncom.2017.00048,
  10.1371-journal.pcbi.1002581, 10.1371-journal.pcbi.1000205, 1901.03048,
  1812.06491, 1708.07390, 1906.00030 (slug = arXiv id with `/`→`-`, else
  first-author-year).
- `inbox-archive.md` shrank 970 → 548 lines; Wave 1–2 region replaced by a
  linked index of the new files. Six `###` pointer stubs kept in place (their
  targets live in *later* archive sections not yet migrated); the Giusti stub
  was re-pointed at `annotations/1502.06172.md` since its target DID move.
- Crossrefs: 22 id-based "Full annotation: ``inbox.md``" pointers across 9
  index files rewritten to `annotations/<slug>.md`; plus 6 name-based pointers
  for Takens/Sauer/Schreiber/Tort (dynamical_systems.md ×2,
  composite_systems.md ×2, phase_transitions.md ×2).
- Lint extended: scans `papers/annotations/*.md` as annotation sources;
  per-paper files must hold exactly one `<id> --- <authors>` header (error if
  0 or ≥2); duplicate-header detection now also catches archive leftovers of
  migrated papers. README tree updated with the annotations/ line.

## Verification
- `python3 scripts/check_structure.py` → exit 0, **0 errors, 1 warning**
  (pre-existing 219-vs-parsed-count debt, resolves at slice completion/A4).
  Note: the A2-era author-year crossref-debt warning is now GONE — all 64
  parsed annotations match an index by explicit id.
- Conservation proof (verbatim move):
  - words in extracted archive region belonging to moved blocks: **5532**
    (`python3` line-range extraction, printed at migration time)
  - `wc -w papers/annotations/*.md | tail -1` → **5536** = 5532 + 4 from the
    two mechanical header promotions (`## Takens (1981)` →
    `## takens-1981 --- Takens (1981)`, same for Sauer; +2 words each). Zero
    body text touched.
- Header promotions: Schreiber (2000) and Tort et al. (2010) were full-depth
  annotations still headed `###` in the archive; promoted to `## <id> --- <authors>`
  grammar in their new files (same normalization class as A2's header pass).

## Findings / surprises
- Index crossrefs already claimed "Full annotation: ``inbox.md``" for Wave 1–2
  papers that actually lived in the *archive* — the pointer layer was stale
  independent of this migration; the slice fixed it for the moved set only.
  Expect the same staleness for Waves 3/4b/Phase-2 when they migrate.
- The six `###` stubs are a mini-index into later archive sections — when
  those sections migrate (slice 2+), each stub must be re-pointed again.
- takens-1981/sauer-1991 had no machine-matchable id anywhere; gave them slug
  ids in their headers and wrote the slugs into the index pointers, which is
  what cleared the author-year-fallback warnings entirely.

## Blockers / for Aaron
- None. Queued subtasks recorded in LOOP_MISSION.md under A3: Blahut+Arimoto
  shared-block split; tighten lint author-year fallback (Wang/Tran classes).

## Next
A3 slice 2: migrate inbox-archive Wave 4b + Wave 3 triage blocks (archive
lines ~425–600 post-slice) to `papers/annotations/`, same mechanics + re-point
the Cohen-Steiner/Adams/Gardner/Sugihara/Tsuda stubs as their targets land.
