# Pass 23 — B2 batch-003 closure — 2026-08-25
**Task:** B2 [in_progress] — finish batch-003: candidates 21–28 (stability-bounds 21–22, null-surrogate 23–26, chain-complex 27–28), pre-annotation ledger dedup check per INDEX.
**Since last pass:** slices 15–17 verified+pushed by orchestrator; corpus at derived 86; batch-003 20/28 consumed.

## What landed
- `7c847bf` slice-18 — candidate-25 promote-on-encounter: Bandt 2020 order-patterns block migrated verbatim from `second_pass.md` SP-13 to `annotations/10.1007-s00362-020-01171-7.md`; 4 crossrefs repointed (dynamical_systems, composite_systems, filtrations, phase_transitions); counts → 87.
- `a6e489c` slice-19 — rejections recorded: candidate-21 (PAC-Bayes meta-learning, one-machine Stability-class), candidate-22 (Raginsky & Recht sensitivity analysis, one machine; flagged notable for machines-first re-triage), candidate-23 (temporal Watts cascade, null-surrogate filter artifact), candidate-28 (zigzag-PH LVLM pruning, PH-as-tool wrapper).
- `63192b4` slice-20 — candidate-24 ANNOTATED as new `annotations/2005.06573.md` (Rindt et al., dHSIC permutation-test consistency; permutation = marginals-preserved null surrogate); filed information_theory + composite_systems; counts → 88.
- `3bd370d` slice-21 — candidate-26 ANNOTATED as new `annotations/10.1111-2041-210X.13985.md` (Caruso et al., max-ent soft-constraint null models for ecological networks; hard-vs-soft constraints read as marginals pinned exactly vs in expectation); filed dynamical_systems + composite_systems; counts → 89.
- `1405222` slice-22 — candidate-27 ANNOTATED as new `annotations/10.1016-j.physrep.2020.05.004.md` (Battiston et al. Physics Reports higher-order networks survey; chain complex + null hypothesis + parameterized homology weak); filed in 4 indices; counts → 90.
- Final commit: LOOP_MISSION ledger updated (batch-003 28/28 FULLY CONSUMED) + this report + INDEX.

**batch-003 is CLOSED: 28/28 consumed (11 annotated/promoted, 17 rejected).**
All three queue batches are now either fully consumed (001, 003) or HELD by
the orchestrator (002 remainder). B2 has no further work until a new queue
batch or the re-triage of held candidates arrives.

## Verification
- Lint before every commit: `python3 scripts/check_structure.py --check`
  → final state `0 errors, 35 warnings` (34 pre-existing gen_stats domain-alias
  notes + 1 from this pass's own annotations — see Findings). Plain lint:
  `0 errors, 1 warning`.
- Counts: `python3 scripts/gen_stats.py` → 90 fully annotated papers, 30 cells,
  min cell 2, 21 cells ≥ 10; README.md + docs/index.html headline counts patched
  to derived truth each slice.
- Content conservation (slice-18 migration): whitespace-normalized diff of the
  SP-13 source block (`awk '/^## SP-13/,/^## SP-14/' second_pass.md`, minus the
  Domain(s)/header lines that became the new file's header) against
  `annotations/10.1007-s00362-020-01171-7.md` body → byte-identical after
  whitespace collapse. No text rewritten or dropped.
- Dedup check ran BEFORE annotating (per INDEX instruction): grep sweep over
  historical ledgers (second_pass/third_pass/cross_domain_bridges) +
  by-domain/by-structure/atlas/glossary for all 8 candidates' author/keyword
  signatures. Only candidate-25 was an existing-coverage artifact.

## Findings / surprises
- Group yield flipped again: info-machines group was 5/6 dedup artifacts;
  these three groups were mostly genuinely new (4 new annotations + 1 promote,
  only 1 dedup hit). The queue-group label predicts little — per-candidate
  triage stays necessary.
- Candidate-22 (Raginsky & Recht, deterministic generalization via sensitivity
  analysis) was rejected under the <2-machines rule but is conceptually close
  to the Stability machine's foundations — recommended for the orchestrator's
  machines-first re-triage pool alongside the held batch-002 candidates.
- New annotation files with non-canonical domains added 1 gen_stats alias note
  ("statistical learning theory, deep learning" on 2305.18887 was already
  there; the +1 warning is from this pass's Domain(s) lines using "machine
  learning" / "information theory" casing variants already in the alias class).
  This is exactly the sanctioned alias-table cleanup surface — 35 informational
  notes now catalogued for that task.
- docs/index.html carries the count in two places (meta og:description + a
  `<span>`); both must be bumped or `--check` fails — noted for future passes.

## Blockers / for Aaron
- None hard. Queue is now empty of unconsumed candidates across batches 001–003;
  next B2 pass needs either a fresh `papers/queue/batch-NNN.md` (refill in
  progress on orchestrator side) or sanction to re-triage the 22 HELD
  batch-002 candidates.

## Next
gen_stats domain-alias-table cleanup (sanctioned task pick): teach
`scripts/gen_stats.py` to map the ~20 recurring free-text domain strings
("machine learning", "optimal transport", "computational neuroscience", …)
onto the 5 canonical domains, driving warnings to 0 without touching
annotation content.
