# Pass 6 — A3 slice 4 — 2026-08-24
**Task:** A3 [in_progress] → **done**. Migrate inbox.md Waves 4+ (34 `##` headers) to per-paper files; reshape inbox.md to contract + leads + wave index.
**Since last pass:** slice 3 landed (4b6db24 + report 98fa9df); orchestrator confirmed verified + pushed.

## What landed
- `aa06188` — A3 slice 4: inbox.md Waves 4c–10c migrated to `papers/annotations/` as 36 verbatim per-paper files (23,826 block words). Includes the Wave 6 papers found under `### DOI: … --- ` subheaders, and the Blahut(1972)+Arimoto(1972) shared block split out of the Chen & Wang block into its own file `blahut-arimoto-1972.md` (header promoted to `## blahut-arimoto-1972 --- Blahut (1972) + Arimoto (1972)`).
- `9d5be1d` — A3 slice 4b: 149 stale `inbox.md` crossrefs in by-domain/by-structure/atlas/glossary repointed to per-paper annotation paths (id-match first, author-year fallback, then nearest-heading context; exactly 1 line needed a manual fix).
- `dec8b5f` — A3 slice 4c: `papers/inbox.md` reshaped to the target layout (ingestion contract + Still-to-find leads + wave index of annotation files); canonical references updated in METHODOLOGY.md, .claude/skills/annotate/SKILL.md, README.md; `check_structure.py` extended with `check_inbox_empty()` — now ERRORS if any `<id> --- <authors>` full annotation remains in inbox.md/inbox-archive.md.
- Ledger: A3 flipped to done. Report: this file. INDEX rewritten.

## Verification
- Lint (`python3 scripts/check_structure.py`) green at every commit: **0 errors, 1 warning** (pre-existing count-source drift: claimed 219 vs 68 parsed headers — resolves at A4's gen_stats).
- Conservation proof for slice 4: every new per-paper file byte-compared against its block reconstructed from `git show HEAD:papers/inbox.md` — 36/36 verbatim except (a) two Wave 6 headers promoted `###`→`##`, (b) trailing `---` separators stripped, (c) header ` --- `→` — `. Word totals: 22,725 content words in files = 22,725 reconstructed from HEAD.
- Mid-slice defect caught and fixed: the migration script's blocks ran to the next *paper* header, so wave-section headers got swallowed into preceding paper files and one Wave-5 pointer bullet was dropped by a later insert script. Both recovered verbatim from HEAD before committing (wave headers restored, all 36 pointers present).

## Findings / surprises
- Orchestrator's "34 headers" undercounts: Wave 6 holds its two papers under `###` subheaders, so the true block count is 36 (+Blahut shared block extracted = 37 new files total incl. blahut-arimoto-1972.md).
- Blahut+Arimoto queued "split" resolved pragmatically: the block is joint prose; splitting would force a rewrite (violates content conservation), so it stays as one shared file under slug `blahut-arimoto-1972`. Flagged to Aaron if a true split is wanted.
- The lint's author-year debt went to 2 after migration (new files lacked id strings in the indexes); fixed by adding id-bearing pointer lines at information_theory.md and optimal_transport.md — debt back to ZERO.
- Remaining warning is purely the claimed-219 figure in README/docs vs 68 actual parsed annotations — A4's `gen_stats.py --check` will settle it.

## Blockers / for Aaron
- None blocking. Decision to review: Blahut+Arimoto kept as a single shared annotation file (see above).

## Next
A4 — stats from data: `scripts/gen_stats.py` deriving paper/cell counts from `papers/annotations/*.md`, regenerating `diagrams/coverage-matrix.md`, patching README + docs/index.html stats, wiring `--check` into the lint.
