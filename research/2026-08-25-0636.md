# Pass 27 — B2/A4-followup gen_stats grid+alias — 2026-08-25
**Task:** orchestrator-directed follow-up to A4/B2: add `statistical_physics` as 6th domain column in gen_stats; alias table for the unrecognized-domain notes; keep `--check` green; NO 7th-machine column (SEPARATRIX still Aaron-gated on PR #1).
**Since last pass:** slice-25 verified+pushed by orchestrator; corpus at derived 97; batch-004 9/17 consumed.

## What landed
- **2c34729** — pass report + ledger + INDEX for the grid/alias work.
- **c68e06c** — B2 slice-27 (budget remained, per orchestrator): batch-004
  rate-distortion candidates 10+11 ANNOTATED (`annotations/2204.01612.md`
  NERD; `annotations/2104.13662.md` Theis–Wagner RDPF), candidate-12
  REJECTED (<2 genuine machines beyond the covered RD/matching lineage);
  queue markers updated. Counts to derived **101**; lint --check 0 errors.
  Conservation: both annotation files are new (+30/+33 lines); index changes
  are appends plus five one-line pointer replacements — no content removed.
- **4fb71b9** — grid/alias follow-up commit:
  - `scripts/gen_stats.py`: DOMAINS gains `StatPhys` (matrix now 36 cells; mermaid 8 columns, SP row); `normalize_domain` rewritten to full-phrase → two-word → first-token matching; DOMAIN_ALIASES extended (18 new keys); new KNOWN_QUALIFIERS set for sub-field parentheticals; machine-bullet matcher accepts the short `Joint-vs-marginal` label form.
  - `scripts/check_structure.py`: hardcoded `/30 cells` in the --check summary now computed from DOMAINS×MACHINES.
  - `papers/annotations/2404.17951.md`: +3 lines only — added the missing `**Domain(s)**: Information theory, machine learning` metadata line (parse gap, not a rewrite).
  - README.md / docs/index.html repointed to derived truth; matrix regenerated.

## Verification
- Before: `--check` exit 0 with 28 informational `gen_stats:` warnings + crossref WARN.
- After: `python3 scripts/check_structure.py --check` → **0 errors, 1 warning** (the standing baseline crossref-debt WARN 2 refs). All 28 alias warnings resolved; gen_stats prints zero notes.
- Conservation: `git show --stat HEAD` — sole annotation diff is `papers/annotations/2404.17951.md | 3 ++` (metadata line added, nothing removed). No annotation prose moved or rewritten anywhere.
- Derived truth now: **99 papers, 36 cells, min cell 2, 23/36 cells ≥10**.

## Findings / surprises (slice-27 additions)
5. **5 more wrong-pointer crossrefs fixed** (same class as slice-24's b777ae5 fix):
   `by-structure/optimal_transport.md` had Peyré 2604.04891, Panaretos–Zemel
   annurev-statistics, and Curry s41468 entries all pointing at
   `annotations/blahut-arimoto-1972.md`; `by-domain/information_theory.md`
   Wave-10a (Baudot 1907.04242) and Wave-10b (Panaretos) ditto. All repointed
   to their real per-paper files. NOTE for Aaron: this suggests a systematic
   Wave-9/10-era copy-paste pointer debt — a lint check that verifies the
   *target* of "Full annotation:" plausibly matches the entry's own title/id
   would catch the class (current lint only checks file existence + author-year
   prose matching).
6. Rate-distortion group now 3/4 consumed (10+11 annotated, 12 rejected,
   13 unconsumed). Both new papers are Matching-machine entries in the
   Blahut–Arimoto lineage — InfoTheo×Matching deepens.

## Findings / surprises
1. **Corpus count moved 97 → 99**, for two legitimate reasons (both flagged here for Aaron):
   - Parser accepted the short bullet label `Joint-vs-marginal` (4 files use it; canonical is `Joint-vs-marginal excess`) — files that previously fell out of the counts entirely ("no recognizable machine bullets") are now counted.
   - 2404.17951 (CS-IB) was fully annotated but missing its Domain(s) line, so it was skipped by derive(); the line was mechanically restored from its own content.
   Both changes make the stats *more* faithful to the corpus, not less; neither edits any annotation semantics.
2. **Alias decisions made** (recorded in DOMAIN_ALIASES comments; Aaron may overrule):
   - `machine learning` / `deep learning` / `statistical learning theory` / `contrastive learning` → **InfoTheo** (every such paper in corpus is IB/MI/InfoNCE theory).
   - `optimal transport`, `algebraic topology`, `computational topology` → **TDA** (matches existing by-domain filing).
   - neuro sub-fields (`neuroimaging`, `fMRI`, `MEG`, `EEG`, `neural decoding`, `motor cortex`, `computational neuroscience`) → **Neuro**; `Granger causality`/`gradient dynamics` → **Dynamics**; `self-organized criticality` → **StatPhys**.
   - Pure qualifiers (`finance`, `applied`, `causal inference`, `complex systems`, `category theory`, `metric geometry`, `algorithms`, `numerical analysis`, `computational biology`, `statistics`, `foundational`, etc.) are recognized-but-not-counted — they describe instantiations, not columns.
3. StatPhys opens as a real but thin column: row = CC 2, PH 2, MA 4, ST 2, JM 5, NH 4 — consistent with the 4 stoch-thermo papers + spillover. No cell ≥10 yet.
4. The "Five Domains" README section and five/six-domains prose in docs were stale against the A6-era direction; updated to six.

## Blockers / for Aaron
- Ratify the alias mappings above (especially ML→InfoTheo and OT→TDA) — one-line overrides in DOMAIN_ALIASES if disagreed.
- SEPARATRIX 7th-machine column remains intentionally OUT (A6 G3 gate, PR #1).

## Next
- batch-004 candidate-13 (Yadav et al., log-likelihood distortion semantic
  compression) closes the rate-distortion group; then ldpc-bp candidates 14–17.
