# Pass 15 — B2 slice-6 — 2026-08-25
**Task:** B2 [in_progress] — consume batch-002 candidates; orchestrator priority: hodge/ldpc/separatrix/spin-glass sections + genuine Dynamics×Matching fits.
**Since last pass:** slice-5 landed (32cc10e): batch-002 opened, candidates 01–03 rejected (geometric-dl wrappers), 5 wrong-pointer crossrefs fixed, counts patched to derived 77.

## What landed
- **85d8d01** — batch-002 triage + one annotation:
  - **candidate-04 [geometric-dl] REJECTED** — GNN classification of clinical-trial protocol documents: an application wrapper instantiating zero atlas machines (<2, rule §4). Same class as pass-14's 01–03.
  - **candidate-07 [ldpc-coding] REJECTED** — Mézard & Mora (2008), arXiv 0803.3061, is *already fully annotated* in `papers/by-domain/information_theory.md` with a rich multi-machine entry. Duplicate, rule §4.
  - **candidate-09 [hodge] REJECTED** — Hodge-Aware Contrastive Learning is already annotated (`by-domain/tda.md`, cross-indexed in boundary_operators / phase_transitions / filtrations). Duplicate, rule §4.
  - **candidate-10 [hodge] ANNOTATED as `papers/annotations/2303.16716.md`** — Topological Point Cloud Clustering (Grande & Schaub). Machines: chain complex (full family of Hodge Laplacians from incidence matrices, not just L_0), matching (points assigned to homology classes), stability (topological robustness vs noise). Dual index added to `by-domain/tda.md` and `by-structure/boundary_operators.md`; matrix regen.
  - README/docs headline counts 77→78 (the `--check` drift gate caught it immediately after gen_stats — the gate is working as designed).
- Ledger (LOOP_MISSION.md B2 line) + INDEX rewritten.

## Verification
- `python3 scripts/gen_stats.py` → wrote diagrams/coverage-matrix.md; derived 78 papers, 20/30 cells ≥10.
- `python3 scripts/check_structure.py --check` → **exit 0, 0 errors**, 25 warnings (all pre-existing gen_stats domain-alias notes).
- Conservation: no migrations this pass — one new annotation file written fresh, three queue status lines flipped per INGESTION §1 (status token + one-sentence reason only), two index entries appended. No annotation content moved or deleted.

## Findings / surprises
- **The orchestrator's sweep is re-surfacing papers already in the corpus.** Candidates 07 and 09 both duplicated existing annotations under slightly different titles/tags than the corpus entries. A title-grep against `papers/annotations/` + index files before queuing would save a pass slot each time. Suggest adding "already-annotated check" to the orchestrator's drop step.
- The `[separatrix]` tag on candidate-08 (HSIC continual learning) looks mis-assigned — the paper has no topological/separatrix content at all; it will likely reject as <2 machines next pass.
- Candidate-11 (normalized Hodge Laplacian Cheeger inequalities, 2604.27241) overlaps heavily with already-annotated Jost & Zhang 2302.01069 (same machine pairing chain-complex+stability); expect a §4 duplicate-reject unless the up/down-case sharpening is judged material.
- Dynamics×Matching remains thin at 4 — none of this pass's candidates touched it.

## Blockers / for Aaron
- None hard. Request to orchestrator: retune the sweep to dedupe against the existing corpus before dropping batches.

## Next
Consume batch-002 candidate-05 (geometric-dl), candidate-08 ([separatrix]-tagged HSIC CL — likely reject), and candidate-11 or 12 (spin-glass-codes) per the ≤3/pass rule; annotate whichever survives triage at full depth. Grep-by-title against `papers/annotations/` first.
