# Pass 24 — B2 slice-23 (batch-004 dyn-matching, candidates 01–03) — 2026-08-25
**Task:** B2 [in_progress] — consume ≤3 papers from oldest unconsumed batch; batch-004 (17 FORAGED candidates, abstract-only provenance) just landed.
**Since last pass:** 5c2a7ad (pass report + batch-003 CLOSED 28/28), 19530f1 (orchestrator queued batch-004). Corpus at derived 90. Baseline lint: 0 errors, 1 warning (2 author-year-prose crossrefs — pre-existing).

## What landed
- `4bed853` — slice-23: batch-004 candidate-01 ANNOTATED as `annotations/2409.08768.md`
  (Botvinick-Greenhouse et al., Measure-Theoretic Time-Delay Embedding — delay embedding as
  pushforward Φ_# between measure spaces, OT machinery; machines matching + stability);
  candidate-02 ANNOTATED as `annotations/1907.08260.md` (Moosmüller–Dietrich–Kevrekidis —
  attractor-reconstruction histories disambiguate OT map identification; matching + weak
  stability/PH); candidate-03 TrajectoryNet REJECTED (one-machine dynamic-OT wrapper,
  superseded by MIOFlow candidate-04). Dual-index filing (dynamical_systems.md +
  optimal_transport.md); queue markers updated; counts regenerated to **derived 92**;
  README + docs/index.html count patched.
- Ledger B2 status line + INDEX rewritten.

## Verification
- `python3 scripts/gen_stats.py` → 92 fully annotated papers, min cell 2, 21 cells ≥10.
- `python3 scripts/check_structure.py --check` → **0 errors**, 37 warnings
  (35 pre-existing domain-alias notes + 2 author-year crossref notes; none introduced here).
- Conservation N/A (no migration — two new files, additive index entries only).
- Queue hygiene lint caught my initial ANNOTATED markers missing ids and the stale 90-counts
  in README/docs — both fixed in-pass before commit. The gate is doing its job.

## Findings / surprises
- **Dynamics×Matching jumped 5 → 7** — the atlas's thinnest cell doubled from a single slice,
  and the two new papers bridge it *in opposite directions*: 2409.08768 goes Dynamics→Matching
  (measure-theoretic Takens, OT as language for the pushforward coupling), 1907.08260 goes
  Matching→Dynamics (delay-history side information to fix degenerate transport maps).
  Together they're the strongest genuine two-machine pair this cell has had.
- **Abstract-only depth caveat (per orchestrator):** both annotations are written strictly from
  stated machinery in the abstracts; stability claims are marked weak/depth-limited in-file.
  If full texts arrive later, re-check the stability sections of both — that's where
  abstract-level confidence is lowest.
- **Pre-existing debt spotted:** docs/index.html hero says "min cell 1" / "18 of 30 cells ≥10"
  while derived truth is min cell 2 / 21 ≥10 — the lint only enforces the paper count on
  index.html, not the cell stats. Candidate follow-up: extend gen_stats --check to cover those
  two strings too.
- TrajectoryNet rejection follows the GWGAN precedent (pass 17); its CNF↔dynamic-OT equivalence
  is real but is instantiated by MIOFlow (candidate-04) with more machinery, so nothing is lost.

## Blockers / for Aaron
- None. Batch-004 remaining groups (stoch-thermo, rate-distortion, ldpc-bp) are all consumable.

## Next
Batch-004 candidate-04 MIOFlow (completes dyn-matching group), then start stoch-thermo (05–09).
