# Pass N — A4 — 2026-08-24
**Task:** A4 [open] Stats from data, not by hand — gen_stats.py derives paper/cell counts
from papers/annotations/*.md, regenerates diagrams/coverage-matrix.md, patches README +
docs/index.html stats, wires --check into check_structure.py.
**Since last pass:** A3 slices 1–4 all landed (1c96d87…dec8b5f); ledger marks A3 done,
68 per-paper annotation files in place, lint at 0 errors / 1 warning (claimed-219-vs-68).

## What landed
- `a49e738` — scripts/gen_stats.py (stdlib only): parses each
  papers/annotations/*.md for its **Domain(s)** line and the machine bullets under
  "Abstract machines instantiated", tallies the domain×machine matrix, rewrites
  diagrams/coverage-matrix.md, prints headline numbers. Matrix regenerated.
- `db97bca` — check_structure.py gains `--check`: imports gen_stats, fails (error)
  if diagrams/coverage-matrix.md differs from a fresh regeneration or if README /
  docs/index.html don't state the derived headline count. README.md +
  docs/index.html patched to derived truth.

**The delta from the old claim:** the site said 219 papers, all 30 cells ≥ 4,
16 cells ≥ 10. Derived truth from full annotations is **68 papers**, min cell **1**
(Dynamics×Matching), **18 cells ≥ 10**. Explanation: 219 counted every index stub
and wave-listed paper ever touched; only 68 have full per-paper annotations after
the A3 migration — Waves 1–3's prose annotations live inside by-domain files as
index entries, not annotation headers. The new counts measure what actually exists
as machine-checkable annotations; the old numbers were aspirational corpus size.

Derived matrix (from coverage-matrix.md):

```
            ChainCmplx ParamHom Matching Stability JointMarg NullHyp
TDA                *29*    *29*     *16*     *28*        7     *17*
QEC                *12*       8        7     *12*        2        9
Dynamics           *10*    *13*        1     *11*        3        7
Neuro              *15*    *19*        4     *14*      *10*     *19*
InfoTheo              7    *16*        4        9      *12*     *13*
```

(`*n*` = ≥10.) Notable real gaps the old "all ≥ 4" hid: Dynamics×Matching = 1,
QEC×Joint-vs-Marginal = 2, Dynamics×JM = 3. These become B2 ingestion targets.

## Verification
- `python3 scripts/check_structure.py` → 0 errors, 0 warnings (exit 0).
- `python3 scripts/check_structure.py --check` → 0 errors, exit 0; prints
  "derived stats: 68 papers, 18/30 cells >=10, min cell 1". The only warnings are
  gen_stats notes about unrecognized *secondary* domain descriptors (e.g.
  "optimal transport", "statistical physics") that aren't one of the 5 canonical
  domains — informational, not debt.
- Drift gate proven live: appended junk to coverage-matrix.md → `--check` exits 1
  with "out of date … run: python3 scripts/gen_stats.py"; regenerating restores exit 0.
- Content conservation: no annotation text was moved or edited this pass — A4 is
  derivation + stat-surface patching only; papers/annotations/ untouched
  (`git show a49e738 db97bca --stat` shows no files under papers/annotations/).

## Findings / surprises
- Annotation files needed three parser accommodations: blank lines inside the
  machine-bullet block; both `**Domain(s)**:` and `**Domain**:` spellings;
  qualifier suffixes on machine bullets ("(weak)", "(inherited)", "(inverted
  direction)") which are stripped before matching. Multi-domain lines split on
  `,` `/` and take the first word of each mention ("TDA / Optimal Transport" → TDA).
- 18 files carry secondary descriptors outside the 5 canonical domains (listed as
  lint warnings under --check). If any should count toward a canonical domain
  (e.g. "neuroimaging" → Neuro), extend DOMAIN_ALIASES in gen_stats.py — one line each.
- The old matrix's "Wave 10 Key Changes" narrative section is gone from the
  regenerated file; history lives in git and inbox.md's wave index, so the emitted
  matrix is now purely current-state data. Aaron may want a changelog pointer if he
  misses it.

## Blockers / for Aaron
- The 68-vs-219 gap means the public headline changed (README/docs now say
  "68 fully annotated papers"). If Aaron wants the 219 corpus-size number visible,
  it needs a different, honest framing (e.g. "68 fully annotated · ~150 index
  stubs") — his call; I didn't invent an uncountable number.
- Real thin cells (Dynamics×Matching=1, QEC×JM=2) are prime Phase-B ingestion
  targets alongside the already-flagged neuroscience cells.

## Next
A5 — loose-file adjudication: read papers/second_pass.md, third_pass_*.md,
cross_domain_bridges.md; fold into canonical structure or document in a
papers/README.md layout guide.
