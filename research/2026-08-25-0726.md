# Pass 35 — B2 batch-006 slice-35 — 2026-08-25
**Task:** B2 [in_progress] Consume queue batches (≤3/pass). Batch-006 opened this pass (8 candidates: gw-theory core ×7 + neuro-pid ×1); consumed candidates 01–03.
**Since last pass:** 4d06b24 (pass-34 report body completed — was left with skeleton placeholders), 03ce262 (orchestrator dropped batch-006), 7669b79/5a0f63d (B3 sub-slice 2 done).

## What landed
- **2ea9370** — batch-006 3/8 consumed, all three ANNOTATED (abstract-only provenance, depth-limited):
  - candidate-01 Weitkamp–Proksch–Tameling–Munk → `annotations/2006.12287.md` (trimmed-GW lower bound as pose-invariant discrimination test with distributional limits; Matching + Null hypothesis).
  - candidate-02 Mémoli–Needham → `annotations/2212.14123.md` (GW = GM exactly on non-atomic mm-spaces; bi-Hölder vs isometry-invariant Monge OT; Kantorovich/Monge split resolved inside the Matching machine).
  - candidate-03 Lim–Mémoli → `annotations/2201.09385.md` (generalized cMDS; negative-eigenvalue-sum non-flatness invariant; cMDS output stable w.r.t. GW on inputs).
- Dual-index filed: `by-domain/tda.md` + `by-structure/optimal_transport.md` (all three) and `by-structure/phase_transitions.md` (01, 03). Queue statuses flipped to ANNOTATED.
- Counts regenerated: derived **121** papers, min cell 2, 27/36 deep. README + docs/index.html patched 118→121 (caught by `--check`).

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors**, 1 warning (2 annotation↔index author-year-prose matches = pre-existing baseline crossref debt, unchanged from pass 33/34).
- Conservation: not a migration — pure additive annotations + index lines; no existing annotation bodies touched (`git show --stat 2ea9370`: 9 files, all new or append-only except README/docs count digits).

## Findings / surprises
- Batch-006's gw-theory core is exactly what the corpus lacked: the Matching machine studied *as itself* (inference theory, Monge-vs-Kantorovich internal split, spectral invariants) rather than via applications. Mémoli–Needham gives a crisp internal fact worth an eventual atlas note: soft matching collapses to hard matching for free once atoms vanish.
- All three are TDA-filed per the OT→TDA alias; none touch neuroscience/statphys cells, so deep-cell count stays at 27 despite +3 papers.

## Blockers / for Aaron
- None.

## Next
- batch-006 candidates 04–06: Chambers–Meng Reeb-graph GW with persistence-image weights + stability theorem (04, likely Matching+Stability+Filtration triple), then 05/06.
