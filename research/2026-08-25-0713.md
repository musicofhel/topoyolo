# Pass 32 — B2 slice-32 — 2026-08-25
**Task:** B2 queue consumption — batch-005: candidate-07 closes channel-capacity; then ph-stability candidates (≤3 papers/pass).
**Since last pass:** slice-31 landed (f1f2dae annotations ×3, 0cf6e45 pointer sweep, e1d4169 report/ledger). Corpus at derived 112, deep cells 27.

## What landed
- **98df75d** — batch-005 channel-capacity CLOSED 7/7 + ph-stability opened 2/5:
  - candidate-07 Ashikaga & Asgari-Targhi → `annotations/1708.03990.md` (capacity/MI/TE as a spatial invariant field locating cardiac order–disorder wavebreaks; applied-dynamics close of the group).
  - candidate-08 Bubenik & Scott → `annotations/1205.3669.md` (persistence diagrams categorified; interleaving distance generalizes bottleneck; stability as categorical property).
  - candidate-09 Gulen & McCleary → `annotations/2201.06650.md` (Galois connections unify interleavings AND matchings; Rota's theorem ⇒ easier bottleneck-stability proof).
  - Dual-index filed: dynamical_systems + neuroscience + tda / filtrations + optimal_transport + phase_transitions; queue headers marked ANNOTATED.
- Counts to derived **115**, deep cells **27**; README/docs/matrix repatched and enforced by --check.

## Verification
- `python3 scripts/gen_stats.py` then `python3 scripts/check_structure.py --check`: **0 errors, 1 warning** (the 2 pre-existing author-year-prose crossrefs).
- Conservation: diff is additive-only — 172 insertions vs 16 deletions, all deletions being the derived-count string replacements in README/docs and the gen_stats matrix regen (`git show --stat 98df75d`). No annotation content touched.

## Findings / surprises
- candidates 08+09 form a tight lineage pair (Bubenik–Scott is Gulen–McCleary's direct ancestor); together they show Matching and Stability are order-theoretically dual in the PH setting — a candidate ANTISYNONYMS/SYNONYMS sharpening for B3 if it survives deeper reading.
- Ashikaga–Asgari-Targhi instantiates only the *pattern* "invariant profile locates transition" — no topology, no optimization; annotated honestly as parameterized-homology-shaped rather than genuine PH. Neuroscience×PhaseTransitions cell fed.
- The ≤3/pass cap binds exactly: ph-stability 10–12 remain for one more pass, closing batch-005 in 3 passes total.
- README line 9 had drifted to "23 cells ≥ 10" while derived truth was 27 — lint only enforces the paper count, not the cell count. Patched this pass; consider extending check_structure --check to assert the deep-cell number too (small A4 follow-up, Aaron-gated).

## Blockers / for Aaron
- None new. Still open from pass 27: ML→InfoTheo and OT→TDA alias ratification.

## Next
- Slice-33: batch-005 ph-stability candidates 10–12 (final three) → batch-005 CLOSED 12/12; sanctioned wrong-pointer sweep ≤5 where catch-alls remain outside qec.md; then B3 trigger is well past met (+42 new since Wave-10 baseline).
