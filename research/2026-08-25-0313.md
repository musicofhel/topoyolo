# Pass 19 — B2 slice-10 — 2026-08-25
**Task:** B2 [in_progress] — batch-003 begins (orchestrator: batch-002 HALTED, remaining 22 HELD-by-orchestrator, not rejected; dyn-matching group first).
**Since last pass:** fe7342a (batch-003 queued, 28 machine-term-filtered candidates), 26a151c/2c3dc7d (slice-9).

## What landed
- **34e8bb2** — batch-003 candidates 01+02 handled by **promote-on-encounter**, not fresh annotation: both papers turned out to ALREADY be fully annotated as prose blocks in the index files (the known ~51-block A3 debt). The queue dedup checked only `annotations/`.
  - candidate-01 (Colbrook, Drmač, Horning 2025, "An Introductory Guide to Koopman Learning", arXiv 2510.22002): prose block in `by-domain/dynamical_systems.md` migrated verbatim → `annotations/2510.22002.md`; crossrefs repointed in `by-structure/filtrations.md` + `by-structure/phase_transitions.md`. Machines per existing block: parameterized homology, stability, matching (spectral).
  - candidate-02 (Zhou, Chu, Xu, Liu, Yu 2020, Electronics 9(5):823): prose block migrated verbatim → `annotations/10.3390-electronics9050823.md`; crossrefs repointed in `filtrations.md`, `phase_transitions.md`, `by-domain/tda.md`. Machines: parameterized homology, null hypothesis, stability.
  - Both source blocks replaced with short pointer stubs; queue file marked ANNOTATED-as; gen_stats regenerated; README/docs counts 78 → derived **80**.
- Ledger updated: batch-002 remaining 22 candidates recorded as **HELD-by-orchestrator** (halt adopted early), not rejected.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors, 27 warnings** (18 domain-alias notes + 9 unrecognized-domain warns — all pre-existing classes, none introduced).
- Content conservation (promote-on-encounter = move, never rewrite):
  - diff of `by-domain/dynamical_systems.md` at HEAD: **1156 words removed** (the two prose blocks);
  - new files total **1190 words**, of which the only additions are the two lint-format headers (`## 2510.22002 --- Colbrook...`, `## 10.3390-electronics9050823 --- Zhou...`) — bodies are byte-verbatim slices of the removed blocks.
- One wrinkle found and fixed during extraction: the Zhou slice initially duplicated its old `##` header line under the new lint-format header; caught on inspection and removed before commit.

## Findings / surprises
- **Queue dedup is insufficient**: "deduped vs annotations/" misses prose blocks living inside by-domain/by-structure files. Both dyn-matching candidates were already in the corpus. Recommend the orchestrator also title-grep `papers/by-domain/*.md` and `papers/by-structure/*.md` when exporting future batches.
- **dyn-matching cell did NOT actually gain a Matching paper.** Candidate-01's "matching" is spectral projection (Laplace averages), not optimal assignment — it lands in Dynamics×Stability/Filtrations. Dynamics×Matching remains thin (=4); no true dyn-matching paper has yet survived triage. The queue's group labels are filter artifacts, not machine verdicts.
- Promote-on-encounter is working well: this slice grew the parseable corpus 78→80 while shrinking the prose-block debt by 2.

## Blockers / for Aaron
- None hard. For orchestrator: widen the dedup grep (see finding 1); consider re-labeling batch-003 groups after triage since group ≠ machines.
- Held batch-002 candidates (22) remain recoverable under a machines-first re-triage — nothing was rejected there this pass.

## Next
- Batch-003 candidates 03–05 [filtration-ph] (lines 177–421 of `papers/queue/batch-003.md`). Triage per INGESTION.md; check BOTH indices for pre-existing prose coverage before annotating anything new.
