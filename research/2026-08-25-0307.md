# Pass 18 — B2 slice-9 — 2026-08-25
**Task:** B2 batch-002, continue from candidate-16 (atlas-general tranche; orchestrator: reject freely, refill is orchestrator's problem; machines-filtered batch-003 in preparation).
**Since last pass:** slice-8 (f8e8b8d, 3b46b92) — candidates 13/14/15 rejected, 15/40 consumed.

## What landed
- `2c3dc7d` — batch-002 candidates **16, 17, 18 triage-REJECTED**, batch-002 now
  **18/40 consumed, 22 queued**. Counts unchanged at derived 78 (no annotations).
  - **candidate-16** — Ein–Küchle–Lazarsfeld 1995 (Seshadri constants, local
    positivity of ample line bundles): pure algebraic geometry, zero machines.
  - **candidate-17** — SNGP (Liu et al., NeurIPS 2020): one machine only on
    strict reading — spectral-normalization Lipschitz bound = Stability;
    distance-awareness/OOD is separatrix-class (not a machine per A6); the
    GP/Laplace output layer adds no second machine.
  - **candidate-18** — Dean "BSM as Flat Limit of Information Geometry"
    (SSRN 6630259, draft v0.18): single-author unpublished draft; at most one
    machine (Stability attractor in the (ν/σ₀,ρ) phase plane); no chain
    complex / matching / null-test machinery.

## Verification
- `python3 scripts/check_structure.py --check` → exit 0, **0 errors, 25 warnings**
  (warnings are the pre-existing gen_stats domain-alias class). First lint run
  after the status flips caught a format error (reason must be a body line under
  the header, not parenthesized into it) — fixed before commit; that is the
  queue-hygiene check working as designed.
- No annotations → no content-conservation proof required (rejections are
  one-sentence status changes per INGESTION §2/§4; batch history preserved).

## Findings / surprises
- Slice-8 previews of 16–18 all held: an **all-reject pass**, second in a row for
  the atlas-general tranche. Running tally for the tranche: **6 rejects / 6**
  (candidates 13–18), directly contradicting the original high-forgeScore note.
- candidate-17 (SNGP) is the closest call of the tranche so far: genuinely
  well-known, real Lipschitz/Stability content. Rejected because the bar is ≥2
  machines and OOD/distance-awareness was ruled separatrix-class by A6. If Aaron
  ever relaxes the two-machine floor to "one strong machine + broad impact,"
  SNGP-class papers would re-enter.
- candidate-18 confirms the draft-quality pattern flagged in pass 17 (SSRN v0.18,
  self-referential prior-work chain).

## Blockers / for Aaron
- None hard. Orchestrator already acting on the tranche-quality flag
  (machines-filtered batch-003) — nothing further needed from Aaron this pass.

## Next
Slice-10: consume candidates 19–21 from batch-002 (still atlas-general tranche;
expect continued low hit rate until batch-003 arrives — consider stopping
batch-002 consumption if 19–21 also all reject, and holding remaining queue for
Aaron's review rather than burning passes on a dead tranche).
