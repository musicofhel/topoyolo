# Pass 11 — B2 slice-2 — 2026-08-25
**Task:** B2 [in_progress] — consume queue batches, ≤3 papers/pass; batch-001 candidates.
**Since last pass:** faf7f4f (slice-1), 6ebfcf2 (B1), orchestrator landed batch-002 (40 candidates).

## What landed
- **fe9443d** — B2 slice-2: triaged 3 batch-001 candidates:
  - **candidate-13 → ANNOTATED** as `papers/annotations/2604.08539.md` (Hu et al. 2026,
    OpenVLThinkerV2 / Gaussian GRPO). Machines: Matching (explicit 1D optimal transport
    coupling forcing advantage distributions onto N(0,1)) + Stability (tail-compression
    outlier robustness); Null hypothesis noted as marginal. Genuine novelty: the
    moment-matching-vs-measure-matching gap stated as RL engineering — same W_1-vs-shape
    territory as QR-DQN. Dual-indexed: `by-domain/information_theory.md` (new Wave 11
    section) + `by-structure/optimal_transport.md` (next to QR-DQN).
  - **candidate-01 → REJECTED**: Walsh-matrix CNN engineering, zero machines.
  - **candidate-14 → REJECTED**: DTW motif discovery = single machine (alignment),
    duplicates 2002.00208 coverage.
  - Stats regen: 70 papers, min cell 2, 19/30 cells ≥10; README + docs/index.html counts patched.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors, 19 warnings**
  (18 pre-existing gen_stats domain-alias notes + 1 new same-class note for
  `2604.08539.md`: "Machine learning" is not a recognized gen_stats domain —
  same informational class as the existing "machine learning" note on 1703.00810).
- Conservation: no migration this pass — one new annotation file plus pointer
  lines; queue edits touched only the three candidate status lines (`UNCONSUMED`
  → `ANNOTATED`/`REJECTED` + one-sentence reason per INGESTION.md §4);
  `git diff` on batch-001 shows 8 changed lines, all status/reason lines.

## Findings / surprises
- Lint's INGESTION §4 check requires the rejection sentence on the line *under*
  the header, not embedded in it — first commit attempt failed lint and was
  reformatted before committing (guard working as designed).
- Batch-001's neuroscience candidates are weak fits so far (candidate-01 was a
  CNN engineering paper mis-tagged `[neuroscience]` because of EEG/ECG test
  beds) — orchestrator may want to tighten source filtering for that tag.
- Candidate-13's author list is OCR-garbled in the queue extract ("Yan Gao-Tian
  Yihe Deng"); annotated as "Hu et al. (2026)" to avoid inventing names.

## Blockers / for Aaron
- None hard. Note for Aaron at merge time: docs/index.html count now 70.

## Next
B2 slice-3: next batch-001 candidates — candidates-02/03 ([neuroscience]) or
candidate-15+ ([dynamics-matching]); grep-by-title first for prose-promotion.
batch-002 (40 candidates) waits until batch-001 is drained.
