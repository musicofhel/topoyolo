# Pass 26 — B2 slice-25 — 2026-08-25
**Task:** B2 [in_progress] — batch-004 stoch-thermo group: candidates 06, 08, 09
**Since last pass:** slice-24 (9a91ba0..b777ae5): dyn-matching closed 4/4; statistical_physics domain opened; counts at derived 95.

## What landed
- `dd857de` — candidates 06/08/09 ANNOTATED: `papers/annotations/2209.00527.md` (Ito 2022, geometric thermodynamics: info geometry ↔ OT via excess EP), `annotations/1408.1224.md` (Barato–Seifert information reservoirs), `annotations/2312.03489.md` (Sekizawa–Ito–Oizumi oscillatory-mode EP decomposition → ECoG); dual-index filing (statistical_physics.md ×3; optimal_transport.md + composite_systems.md ×2); counts patched to derived 97 in README + docs/index.html (were stale at 92).
- queue markers flipped to ANNOTATED; stoch-thermo group CLOSED 4/4. batch-004 now 9/17 consumed.

## Verification
- `python3 scripts/check_structure.py` → **0 errors, 1 warning** (2 author-year-prose crossrefs = pre-existing baseline debt).
- `python3 scripts/check_structure.py --check` → **0 errors** (44 informational domain-alias notes).
- Conservation: no migration this pass — all three annotations are new files (net additions only; index edits are appended entries, no text moved or deleted). `git show --stat dd857de`: 3 new annotation files, 3 index appends, README/docs count patches.
- Counts: gen_stats derived 97 papers = 94 (start of pass) + 3 new annotations. Pass-24/25 ledger lines claiming "derived 94/95" were consistent with the annotation files but README/docs had not been re-patched past 92.

## Findings / surprises
- **README + docs/index.html were stale at "92 fully annotated papers"** before this pass even though pass 24/25 reported lint green with counts at 94–95 — the headline-count lint (`--check`) evidently wasn't catching it because the last full `--check` run predates those passes' final commits. Both now state the derived 97 and `--check` enforces it. Worth a glance at whether slice-24's report over-claimed lint state.
- **Orchestrator question re gen_stats 5-domain grid**: YES, the alias/grid follow-up task should land BEFORE statistical_physics is confirmed. Evidence: gen_stats emits 44 "unrecognized domain(s)" notes, including `quant-ph-0110143.md: unrecognized domain(s): statistical physics` — i.e., a paper already declares the exact domain name that batch-004 opened as a new index file, but gen_stats' 5-domain vocabulary doesn't know it. The matrix still reports "30 cells (6 machines × 5 domains)", so statistical_physics-domain papers are counted into whichever of the 5 legacy domains their free-text matches, not into a 6th column. If Aaron confirms the domain, gen_stats needs a 6th column + alias table (the A6 SEPARATRIX follow-up would add a possible 7th column too — one task could do both).
- The three new papers form an Ito-lineage cluster with 05+07: five stoch-thermo papers where matching cost, joint-vs-marginal excess, and null-model reasoning all appear *in dissipation units*. Strong candidate for a B3 atlas synthesis touch-up on optimal_transport.md + composite_systems.md ("thermodynamic instantiations" subsection) once batch-004 closes.

## Blockers / for Aaron
- statistical_physics domain confirmation (carried). Recommendation: confirm, then commission the gen_stats alias/grid follow-up (6th column + unrecognized-domain reconciliation, possibly bundled with the A6 7th-machine column).
- Nothing else queued-blocked: batch-004 has rate-distortion (10–13) and ldpc-bp (14–17) groups remaining.

## Next
- Slice-26: consume batch-004 candidate-10 first of the rate-distortion group (≤3 papers), same pipeline; if any rate-distortion paper is Neuroscience-relevant, note it for the weakest-cell priority.
