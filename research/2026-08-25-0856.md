# Pass 51 — B2 batch-009 close (candidate-10) + B3 pairing note — 2026-08-25
**Task:** B2: annotate candidate-10 (1906.05212 skeptical null) AS A PAIR with
1410.3831, discharge the B3 pairing flag, close batch-009. Then pick up the
open B3 hook (claim-vs-refutation pair note).
**Since last pass:** bd28361 (pass-50 report+ledger), 2db5157 (rg-dl 07–09,
corpus 160).

## What landed
- `2673dd9` — batch-009 CLOSED 10/10: candidate-10 annotated as
  `papers/annotations/1906.05212.md` (de Mello Koch ×2 & Cheng 2019,
  hidden-visible-correlator diagnostics; machines: null hypothesis core +
  stability + parameterized homology-as-test-bench). Annotated AS A PAIR with
  1410.3831: mutual See-also links, B3 pairing flag discharged on both sides
  (annotation flag → "DISCHARGED", domain-file flag → discharged pointer).
  Claim-vs-refutation antisynonym filed in `glossary/ANTISYNONYMS.md`
  ("Exact RG↔DL mapping ≠ correlator-level identity"). Cross-ref lines added
  to by-domain/statistical_physics.md + filtrations.md + phase_transitions.md.
  gen_stats regen + README/docs count patch same commit (derived 161, deep 32).
- `a388d09` — B3 sub-slice 6 (second open hook from INDEX): ANTISYNONYMS note
  "Abrupt order-parameter jump ≠ phase transition" folding in 2505.10114
  (extreme-sync finite-N bifurcations as null support for transition-vs-
  bifurcation inference), cross-linked to the rg-dl criticality-claim caution.

## Verification
- `python3 scripts/check_structure.py` → **0 errors, 1 warning** (the
  pre-existing baseline of 2 author-year-prose crossrefs). Annotations parsed:
  **161**. Run before each commit; exit 0 both times.
- Content conservation: no annotation text deleted or summarized anywhere.
  The only rewrites are the two B3 *flag* sentences on 1410.3831
  (annotation + domain file): "pair with … when it lands" → "paired with …
  DISCHARGED (pass 51)" — bookkeeping status change, full content retained and
  extended with a pointer to the new pair note. New content is purely additive:
  1 new annotation file, 4 index lines, 2 glossary sections (~450 words),
  queue header/status lines.

## Findings / surprises
- The pairing is unusually clean for the corpus: 1906.05212 doesn't refute
  Mehta–Schwab's theorem, it refutes the *strong reading* of it (mapped
  formalism ≠ instantiated dynamics). That distinction is now written up as
  a general corpus rule inside the antisynonym note ("for any 'X is Y' claim,
  distinguish mapped formalism from instantiated dynamics").
- 1906.05212 gives rg-dl its first entry whose headline is a qualification of
  the lineage's own founding claim — the null-hypothesis machine appears
  *within* a single lineage, not just across domains.
- docs/index.html line 27 still shows ">160" (a ">N" display, not an equality)
  — left as-is; will read true until ~170.
- Lint warning count unchanged at 1 (2 prose-matched crossrefs) — baseline
  debt, untouched.

## Blockers / for Aaron
- None this pass. batch-009 fully closed; B2 is IDLE until batch-010 lands in
  papers/queue/ (orchestrator foraging, per notes).

## Next
- When batch-010 arrives: consume per INGESTION (≤3/pass). If idle instead,
  remaining B3 hooks: possible ANTISYNONYMS note from 1910.05979
  (positivity+identity as a lattice property), then the post-batch-008
  dynamical_systems.md atlas re-read.
