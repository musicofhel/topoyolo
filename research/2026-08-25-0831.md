# Pass 47 — B2 batch-008 close + B3 bridge-pair slice — 2026-08-25
**Task:** B2 close batch-008 (kuramoto candidates 10–11), then queued B3
dynamical_systems bridge-pair slice (2108.05024 × 2409.08768).
**Since last pass:** 3307f7d (pass 46: kuramoto 07–09 annotated, corpus → derived 149,
deep cells 30). Orchestrator confirms kuramoto 07–09 verified + pushed upstream.

## What landed
- **b0966e1** — B2 pass 47: kuramoto candidates 10–11 ANNOTATED; **batch-008 CLOSED 11/11**.
  - `annotations/2109.08918.md` — Song, Choi & Kahng (2021), ML approaches for Kuramoto:
    learned order parameters as hybrid-sync transition/criticality detectors (stability core);
    network structure inferred from chaotic patterns = inverse identification of the coupling
    graph from dynamics (matching weak) — same inference direction as the reservoir-GS cluster.
    Filed statistical_physics + dynamical_systems / phase_transitions + optimal_transport.
  - `annotations/1305.1742.md` — Witthaut & Timme (2013), Kuramoto dynamics in Hamiltonian
    systems: exact embedding of dissipative Kuramoto on invariant manifolds of a conservative
    2N system; sync transition = transverse instability of the action dynamics. Machines:
    stability (core — new computation route for the same boundary), joint-vs-marginal excess
    (transverse dynamics = composite-level excess over the Kuramoto marginal). Filed
    dynamical_systems + statistical_physics / phase_transitions + composite_systems.
  - Queue file marked ANNOTATED for both; gen_stats regen; README/docs counts patched in the
    SAME commit (sanctioned protocol): derived **151** papers (+2), deep cells **31**, min cell 2.
- **a57fc1e** — B3 sub-slice 5: `atlas/MATCHING.md` new section "Same theorem, two machines":
  Takens via conjugacy (2108.05024, Lagrangian/pointwise, certified by conditional-Lyapunov
  gap per 2401.00885 + dimension bound per 2501.11357) vs via pushforward (2409.08768,
  Eulerian/measure-coupling, robust by measure degradation). Framed as one Matching machine
  surviving a change of description level while its instances shift; complementary Stability
  certificates on each side. Both annotations' See-also repointed to the section.

## Verification
- `python3 scripts/gen_stats.py` → 151 papers, 36 cells, min cell 2, 31 cells ≥10.
- `python3 scripts/check_structure.py --check` after both commits: **0 errors,
  1 warning** — the warning is the pre-existing baseline (2 author-year-prose
  crossrefs, catalogued since A2). One transient error class during the slice
  (stale README/docs counts before patching) was fixed in-commit per protocol.
- Content conservation: this pass ADDED annotation files and index entries; no
  existing annotation content was moved or rewritten (only See-also pointer lines
  extended). Annotation count via lint parse: 151 = 149 + 2 new files.

## Findings / surprises
- 2109.08918's "ML-as-instrument" domain qualifier is not in KNOWN_QUALIFIERS;
  rather than extend the table unilaterally I dropped the parenthetical (the paper
  is genuinely statphys/dynamics). If Aaron wants an ML qualifier class, that is a
  gen_stats table change awaiting ratification alongside ML→InfoTheo.
- 1305.1742 gives a rare *route change* for a Stability result (same boundary,
  computed from a Hamiltonian transverse instability) — good candidate material if
  the atlas ever wants a "same invariant, two computations" section parallel to
  the new MATCHING one.
- Batch-008 fully consumed with zero rejections — first all-ANNOTATED batch.

## Blockers / for Aaron
- Nothing blocking. Batch-009 (foraging) is on the orchestrator side; no queue
  work remains locally. ML-domain qualifier question noted above (low priority).

## Next
- B2 idles until batch-009 lands in `papers/queue/` (≤3 papers/pass).
- Meanwhile, open B3 options: (a) pass-46 hook — fold 2505.10114 into
  glossary/ANTISYNONYMS.md as authorial support for the transition-vs-bifurcation
  null entry; (b) catch-all wrong-pointer debt sweep (~100 residual, ≤5/pass);
  (c) atlas re-read of dynamical_systems.md now that batch-008 closed.
