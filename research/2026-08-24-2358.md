# Pass 9 — B1 (+ B2 slice-1) — 2026-08-24
**Task:** B1 [open] Ingestion contract; then first B2 sub-slice per the one-sub-slice rule.
**Since last pass:** A6 done (4e85b19) — Phase A closed; orchestrator exported queue batch-001 (21 candidates).

## What landed
- `6ebfcf2` **B1 ingestion contract.** `papers/INGESTION.md`: queue format (`## candidate-NN [tag] — STATUS` with UNCONSUMED / `ANNOTATED as <id>` / REJECTED + one-sentence reason), per-pass batch rule (≤3 papers), 7-step per-paper pipeline, triage-reject rule (<2 machines or duplicate coverage). `check_structure.py` gained `check_queue_hygiene()` (status validity, id uniqueness, ANNOTATED-without-id, REJECTED-reason presence); INGESTION.md documented in papers/README.md layout contract. All three negative tests fire correctly (bogus status / REJECTED-without-reason / ANNOTATED-without-id).
- `faf7f4f` **B2 slice-1 (candidate-21).** See surprise below — this was a PROMOTION, not a fresh annotation: existing prose annotation of Amornbunchornvej, Zheleva & Berger-Wolf (2020) moved verbatim from `by-domain/information_theory.md` to `annotations/2002.00208.md` (canonical ` --- ` header); pointer left in by-domain; by-structure pointers fixed in composite_systems.md and added under optimal_transport.md §Dynamical Systems (matching machine filing was missing); batch-001 candidate-21 → `ANNOTATED as 2002.00208`; matrix regen.

## Verification
- `python3 scripts/check_structure.py --check` → **0 errors, 18 warnings** (the 18 are pre-existing gen_stats domain-alias notes, catalogued since A4). Negative tests for the new queue check all errored as designed, then re-ran clean.
- Content conservation (B2 slice): extracted block = 421 words → `papers/annotations/2002.00208.md` = 421 words (only the header separator changed `—` → ` --- `; zero prose edits). Commands: python block-extraction script + `wc -w`.
- Count propagation: derived corpus went 68 → 69; README.md + docs/index.html patched to "69 fully annotated papers · min cell 2 · 19 cells ≥ 10" (min cell improved 1→2 because this promotion filled a cell).

## Findings / surprises
1. **A3 migration debt rediscovered and now measured: 51 full annotations still live as prose blocks inside by-domain/by-structure index files** (headers of the form `## <id> — Authors`, em-dash, which evades the lint's ` --- `-based HEADER_RE). None have per-paper files. Candidate-21 turned out to be one of them, so its "consumption" was a verbatim promotion. This is exactly the A5-carried class (Mollers/Mézard) but ~50× larger. The lint cannot see them today.
2. Consequence for B2: several queue candidates may already exist as prose annotations in the indices — each consumption must start with a title/id grep before writing anything new, else we duplicate the corpus.
3. Queue quality is mixed: many neuroscience candidates (01–03, 07, 11–12 etc.) look like single-machine applied ML/EEG papers likely to be triage-rejected under INGESTION.md §4; candidates 14, 18, 21 (dynamics-matching cluster) look strongest.

## Blockers / for Aaron
- None hard. Recommendation: add an A3-followup task to promote the remaining 50 prose annotations to per-paper files (mechanical moves, content conservation per block) and extend check_structure.py to fail on em-dash annotation headers in index files so the class can't regrow. Until then B2 passes should keep promoting on encounter.

## Next
Next pass = B2 slice-2: grep-check candidates 20 and 18 against the 51-block prose debt, promote-or-annotate up to 3 papers from batch-001 (candidate-18's DTW approximation bound is the top pick if unconsumed elsewhere).
