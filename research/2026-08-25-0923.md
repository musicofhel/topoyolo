# Pass 55 — B3 sub-slice 8 (STABILITY.md tri-incarnation) — 2026-08-25-0923

**Task:** B3 sub-slice 8 — atlas/STABILITY.md tri-incarnation section + sanctioned
one-line 'TDA, mathematics' domain-line warning fix.

**Since last pass:** e8f8288 (pass-54 report), d7d2885 (B3 sub-slice 7
CHAIN_COMPLEX sheaf synthesis), fd2a4d8 (batch-010 closed 9/9, corpus 170).

## What landed

- **e6f2f9e** — atlas/STABILITY.md: new section "Three Incarnations of the
  Stability Claim" placed after the 7-flavors paragraph, before Key Divergences.
  Orthogonal axis to the flavors (flavors = form of the bound; incarnations =
  *what object is stabilized*):
  1. **Invariant** (TDA): bottleneck/interleaving — metric inequality between
     invariants; cites math/0604068 lineage plus `annotations/1205.3669.md`
     (categorified interleaving) and `annotations/2201.06650.md` (Galois
     connections unify interleaving+matching).
  2. **Trajectory correspondence** (Dynamics): Lyapunov/GS — contraction rates;
     GS=ESP (`annotations/2506.22335.md`), conditional-Lyapunov spectral-gap
     margin (`annotations/2401.00885.md`), Banach ρ<1 special case.
  3. **Structure itself** (GIT): moment-map semistability from
     `annotations/2605.11178.md` — oversmoothing as orbit-closure degeneration,
     equal-stalk stability wall as structural null.
  Plus a composition note: GIT certifies boundary data → TDA certifies the
  diagram computed from it → Lyapunov/GS certifies faithful reading of dynamics
  on it; failures at each level are invisible to the others.
- **Same commit, sanctioned cleanup:** scripts/gen_stats.py adds `"mathematics"`
  to KNOWN_QUALIFIERS (field descriptor, not a domain column) — clears the
  2505.23993 unrecognized-domain warning. One line, no tooling semantics changed.

## Verification

- `python3 scripts/check_structure.py --check`: **0 errors, 1 warning**
  (down from 2 — the gen_stats mathematics warning is cleared; remaining WARN is
  pre-existing baseline crossref debt: "2 annotation<->index references matched
  only by author-year prose"). Derived stats unchanged: 170 papers, 32/36 cells
  ≥10, min cell 2.
- No content migration this pass (synthesis prose only), so no conservation check
  required; all cited annotation files referenced by path, nothing moved or
  rewritten.

## Findings / surprises

- The tri-incarnation framing composes as a pipeline (structure → invariant →
  trajectory reading), which suggests a possible future ANTISYNONYMS entry:
  "GIT semistability ≠ persistence stability" (orbit-closure vs Lipschitz —
  different partial orders, no metric comparison between them). Not filed this
  pass; left as an open hook for Aaron's call.

## Blockers / for Aaron

- None. Batch-011 foraging is on the orchestrator side; B2 idles until it lands.

## Next

B2 resumes with batch-011 when papers/queue/batch-011.md lands. Open B3 hooks:
possible ANTISYNONYMS above; dynamical_systems.md atlas re-read post-batch-008
(still queued from sub-slice 7 notes).
